# AGENTS.md — Glosslay

このファイルは AI コーディングエージェント（Claude Code / Codex 等）向けの入口です。
**作業を始める前に、必ず `RULES.md` を読んでください。**

---

## プロジェクト概要

**Glosslay**（グロスレイ）は、日本語未対応のPCゲームをプレイしながら、
画面の外国語テキストをOCRで読み取り、翻訳してオーバーレイ表示する Windows デスクトップアプリです。

名前の由来: `gloss`（言語学で、外国語の文に添える対訳注釈）+ `overlay`

**目的**: 必要な説明やセリフが読めず、ゲームの内容を理解できない状態を解消する。

---

## ドキュメント構成

### リポジトリ内（エージェントが常に参照するもの）

| ファイル | 内容 | いつ読むか |
|---|---|---|
| **`RULES.md`** | **絶対的な制約と禁止事項** | **必ず最初に。毎回参照する** |
| **`SPEC.md`** | **実装仕様（リポジトリ内の正）** | 実装対象の機能を確認するとき |
| `PLAN.md` | フェーズ別の実装計画とタスク | 次に何をやるか決めるとき |
| `README.md` | プロジェクト概要（人間向け） | ─ |

### リポジトリ外（人間が管理する原典）

**完全な要件定義書はこのリポジトリの外にあります。**

```
C:\Users\arink\source\99.Cloude資料\009_ゲーム翻訳アプリ\01.要件定義\glosslay-spec.md
```

約90KB。全ての設計判断の根拠、却下した選択肢とその理由、契約・法務チェックリスト、
決定ログが含まれます。**リポジトリ外のため、通常はアクセスできません。**

> **実装作業では `SPEC.md` をリポジトリ内の正として扱ってください。**
> 要件定義書の参照が必要になった場合は、その旨を人間に伝えてください。
> 判断根拠を知りたい場合も、まず `RULES.md` と `SPEC.md` に答えがないか確認してください。

> ⚠️ **要件定義書をこのリポジトリにコピー・コミットしないでください**（`RULES.md` 🔴-4）。
> 一度コミットすると git 履歴に残り、後から削除しても辿れてしまいます。

---

## リポジトリ構成（GitHub）

**2リポジトリ構成を採用します。**

| リポジトリ | 公開設定 | 中身 |
|---|---|---|
| `glosslay`（本リポジトリ） | **Private** | ソースコード。**仕様書は含めない** |
| `glosslay-releases` | **Public** | README と Releases のみ。**exe だけを置く** |

**理由**: Private リポジトリでは Releases も認証必須になり、一般ユーザーが exe をダウンロードできません。
一方 Public にすると全ファイルと git 履歴が公開されます。
公開ダウンロードと非公開ソースを両立させるため、配布専用の Public リポジトリを分離します。

**配布の主軸は `arinkolab.com/apps/glosslay/`**、GitHub Releases はミラー兼「信頼の借用」（未署名対策）です。

> **本リポジトリを Public にする予定はありません。**
> OSS向けの安価な署名証明書（Certum Open Source 等）は「非商用」が条件であり、
> 広告・サブスクを予定する本プロジェクトでは使用できないため、OSS化の動機がありません。
> 署名は Certum Standard（個人向け・ソース非公開可）を使う方針です。

---

## 技術スタック（確定済み・変更禁止）

| 項目 | 採用 |
|---|---|
| 言語 / ランタイム | **C# / .NET 10 (LTS)** ※.NET 8/9 は 2026-11-10 サポート終了 |
| UI | **WPF 単体**（WinUI 3 / WebView2 ハイブリッドは却下済み） |
| 対象OS | **Windows 11 のみ**（x64） |
| 画面キャプチャ | **Windows.Graphics.Capture (WGC)** |
| オーバーレイ | **外部レイヤードウィンドウ**（Win32） |
| キーボード入力 | **Raw Input (WM_INPUT)** |
| ゲームパッド | **Windows.Gaming.Input**（読み取り専用） |
| OCR | **PaddleOCR (PP-OCRv6)** 既定 / **Windows.Media.Ocr** 高速モード |
| 翻訳 | **ローカルNMT (Opus-MT系 int8 ONNX)** 既定 / **Gemini Flash-Lite系** 上位 |
| 推論基盤 | **ONNX Runtime**（OCRと翻訳で共有する） |

---

## 命名規約

- 名前空間 / アセンブリ名: `Glosslay`
- ユーザーデータ保存先: `%APPDATA%\Glosslay\`
- 配布URL: `https://arinkolab.com/apps/glosslay/`

---

## 作業の進め方

1. `RULES.md` の禁止事項に抵触しないか確認する
2. `PLAN.md` で現在のフェーズと次のタスクを確認する
3. 実装する（`SPEC.md` の該当する FR-xxx を満たすこと）
4. `PLAN.md` のチェックボックスを更新する
5. 仕様の解釈で迷ったら **推測で進めず、要件定義書を確認するか人間に質問する**

### 仕様変更が必要になったとき

**勝手に仕様を変えないでください。** 実装上の問題で仕様通りに作れない場合は、
作業を止めて問題点と代替案を報告してください。

承認された変更は以下の**両方**を更新する必要があります。

1. リポジトリ内の `SPEC.md`（エージェントが更新する）
2. リポジトリ外の要件定義書の決定ログ（**人間が更新する**）

**2つのファイルが乖離しやすい構成なので、変更時は必ず人間に「要件定義書側の更新」を伝えてください。**

---

## 現在のフェーズ

**v0.1 PoC** — 詳細は `PLAN.md` を参照。

**PoC の目的は「作ること」ではなく「PaddleOCR で実ゲームのテキストが読めるかを検証すること」です。**
ここが成立しなければ、他の全ての設計が無意味になります。
UI の作り込みや設定画面は PoC の対象外です。
