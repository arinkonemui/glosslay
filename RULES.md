# RULES.md — Glosslay 実装ルール

**このファイルの内容は絶対です。例外を作る場合は必ず人間の承認を得てください。**

---

## 🔴 絶対禁止（プロジェクトが破綻する）

### 1. ゲームプロセスへの干渉を一切行わない

```
禁止: DLLインジェクション
禁止: ゲームプロセスのメモリ読み取り / 書き込み
禁止: DirectX / Vulkan のフックによる描画
禁止: ゲームプロセスへのアタッチ、ハンドル取得
```

**理由**: アンチチートによるBANリスク。ユーザーがゲームアカウントを失う。
主要アンチチートは「メモリを読まない外部オーバーレイ」を許容しているが、
インジェクションは明確に検知・BAN対象。**これは恒久的な制約であり、性能上の理由でも緩和しない。**

### 2. 難読化・パッカーを使用しない

**理由**: 画面キャプチャ＋入力監視という構成は、それだけでAVの誤検知を受けやすい。
難読化はヒューリスティック検知を確実に誘発する。
**ライセンスロジックの保護よりAV誤検知の回避を優先する。**
保護はサーバー側の署名検証（公開鍵をクライアントに置く方式）で担保済み。

### 3. ユーザーの入力を奪わない

```
禁止: RegisterHotKey（キーがゲームに届かなくなる）
禁止: WH_KEYBOARD_LL 低レベルフック（キーロガー誤検知のリスク）
禁止: ゲームパッド入力のブロック / 書き換え / 消費
```

**必須**: キーボードは **Raw Input (WM_INPUT)**、ゲームパッドは **Windows.Gaming.Input で読み取りのみ**。

### 4. 要件定義書をリポジトリにコミットしない

要件定義書はリポジトリ外（`99.Cloude資料\...\01.要件定義\`）で人間が管理しています。
**設計判断の根拠・収益計画・法務メモを含むため、公開してはいけません。**

```
禁止: 要件定義書をリポジトリ内にコピーしてコミットする
禁止: 要件定義書の内容を大量に SPEC.md 等へ転記する（判断根拠・収益計画・法務メモの部分）
禁止: このリポジトリを Public にする判断を独断で行う
```

**一度コミットすると git 履歴に残り、後から削除しても履歴を辿れば読めてしまいます。**
リポジトリを Public にする判断は人間のみが行います。

### 5. 非公式APIやスクレイピングを使わない

```
禁止: 翻訳サービスのWeb版を非公式に叩く
禁止: User-Agent 偽装
```

**理由**: 利用規約違反。配布ツールでは採用不可。

---

## 🟠 必ず守る実装制約

### オーバーレイウィンドウのスタイル

以下は**すべて必須**。1つでも欠けると要件を満たさない。

| スタイル | 目的 |
|---|---|
| `WS_EX_LAYERED` | 透過表示 |
| `WS_EX_TRANSPARENT` | **クリックスルー**（クリックが下のゲームに透過する） |
| `WS_EX_NOACTIVATE` | **ゲームのフォーカスを奪わない** |
| `WS_EX_TOOLWINDOW` | Alt+Tab の一覧に出さない |
| `TOPMOST` | 最前面維持 |

### Windows 11 前提 — レガシー互換コードを書かない

```
不要: GDI BitBlt / PrintWindow によるキャプチャ
不要: DXGI Desktop Duplication のフォールバック
不要: XInput / DirectInput / Raw Input(HID) によるパッド対応
不要: .NET Framework 互換のための記述
```

**WGC と Windows.Gaming.Input で十分。二重実装は保守コストにしかならない。**

### 管理者権限を要求しない

昇格が必要な設計にしないこと。

### GPU推論は既定で無効

ゲームとVRAMを奪い合う。CPU推論を既定とし、スレッド数の上限を設定可能にする。

---

## 🟡 設計上の原則

### 1. 既定でキー登録なしに動くこと

**ローカルNMTが既定の翻訳バックエンド。** APIキーがなくても全機能が動作すること。
クラウド翻訳（Gemini）は「精度を上げたい人向けの任意のアップグレード」。

**この順序を逆にしない。** Googleが無料枠を縮小してもツールが機能し続けるための設計。

### 2. 外部依存はすべてインターフェース越しにする

| インターフェース | 実装を差し替える理由 |
|---|---|
| `IOcrEngine` | PaddleOCR / Windows.Media.Ocr の切替とフォールバック |
| `ITranslator` | ローカルNMT / Gemini / OpenAI互換の切替 |
| `IEntitlementService` | 無料版 / サブスク版の機能ゲート |
| `IAdProvider` | 広告の有効化（v1.0 では常に `NullAdProvider`） |
| `IRemoteConfig` | 広告構成・推奨モデル名の遠隔更新 |
| `IInputTrigger` | キーボード / ゲームパッド |

### 3. モデル名をハードコードしない

**Gemini のモデル名は設定値として保持し、`IRemoteConfig` で更新可能にすること。**
理由: LLMの世代交代が速い（例: Gemini 2.5 Flash-Lite は 2026-10-16 提供終了予定）。

### 4. 機能ゲートは必ず `IEntitlementService` を経由する

v1.0 の実装は常に無料版を返すが、**判定箇所は全て実装しておく**。
後から差し込むとUI全体に手が入る。

### 5. v1.0 で無料提供した機能を、後から有料側へ移さない

有料化は「新しい機能を足す」形でのみ行う。
**特に PaddleOCR とローカルNMT は既定機能であり、有料化してはならない。**

### 6. 失敗時は安全側に倒す

| 状況 | 挙動 |
|---|---|
| クラウド翻訳が 429 / ネットワークエラー | **静かにローカルNMTへフォールバック**して継続（止めない） |
| PaddleOCR の初期化失敗 | Windows.Media.Ocr へ自動フォールバック + UIに表示 |
| リモート設定の取得失敗 | **広告なし**として扱う（誤って表示しない） |
| ライセンスの猶予期間切れ | **機能停止ではなく無料版へ降格** |

### 7. 数値の正確性を守る

**原文から数値を抽出し、訳文に同じ数値が含まれるか機械的に照合すること。**
不一致なら警告表示または原文併記に切り替える。

理由: スキル説明の「ダメージ50」「クールダウン12秒」を誤ると、
翻訳として流暢でも情報として有害になる。LLMは数値を書き換えることがある。

### 8. ユーザーに予期しない課金を発生させない

- 差分がなければOCR・翻訳をスキップ
- 同一原文の結果をキャッシュ
- 自動監視の呼び出し回数に上限（既定で有効）
- 使用量を常時表示

---

## 🔵 コーディング規約

- 非同期処理は `async/await`。UIスレッドをブロックしない
- P/Invoke は専用クラスに集約し、直接散らさない
- 設定は人が読める JSON で `%APPDATA%\Glosslay\` に保存
- ログは既定でローカルのみ。送信は明示的な同意ベース
- 例外を握り潰さない。ユーザーに見せるメッセージは具体的に

### 依存関係を増やすときは相談する

**配布サイズとAV誤検知に直結するため、NuGetパッケージの追加は安易に行わないこと。**
ONNX Runtime のように既に決定済みのものを除き、新規追加は理由を添えて報告する。

---

## ⚫ 迷ったときの判断基準

**「ゲーム体験を壊さないこと」が常に最優先。** 翻訳精度や機能量より上位。

判断に迷ったら、この順で優先する。

1. ユーザーのゲームアカウントを危険に晒さない
2. ゲームの操作・描画を妨げない
3. ユーザーに予期しない費用を発生させない
4. 翻訳の正確さ（特に数値）
5. 機能の豊富さ

**推測で実装を進めないこと。** 仕様が不明瞭なら要件定義書を確認し、
それでも不明なら作業を止めて質問する。
