# SPEC.md — Glosslay 実装仕様

**本ファイルがリポジトリ内の正です。実装作業ではこれを参照してください。**

完全な要件定義書（約90KB / 設計判断の根拠・却下した選択肢・法務チェックリスト・決定ログを含む）は
**リポジトリ外**で人間が管理しています。

```
C:\Users\arink\source\99.Cloude資料\009_ゲーム翻訳アプリ\01.要件定義\glosslay-spec.md
```

> ⚠️ **本ファイルを変更した場合、要件定義書側も人間が更新する必要があります。**
> 自動同期されないため、変更時は必ずその旨を人間に伝えてください。

---

## 1. 対象範囲

**日本語未対応のPCゲーム全般**（PvP / PvE / RPG / アドベンチャー / カードゲーム / シミュレーション等）。ジャンルは限定しない。

### 対象とするテキスト

| # | ユースケース | 特性 | 動作モード |
|---|---|---|---|
| UC-1 | RPG・アドベンチャーのセリフ | 文章。位置固定。操作で送られる | **自動監視**が向く |
| UC-2 | スキル・アイテム・装備の説明 | 装飾フォント。**数値が重要** | 手動トリガー |
| UC-3 | **カードゲームのカードテキスト** | イラスト背景・小さい文字。**最難関** | 手動トリガー |
| UC-4 | クエスト説明・チュートリアル | 文章量が多い | 手動トリガー |
| UC-5 | メニュー・UI項目 | 高コントラスト。容易 | 手動トリガー |

### 対象としないもの

- **切り替わりが速すぎるテキスト**（戦闘中の実況ログ、流れるチャット等）
- 縦書きテキスト
- 音声認識・音声翻訳
- ゲームファイルの書き換え、翻訳MOD生成

> **速いテキストを追わないと割り切ることで、速度より精度に振る判断が正当化されている。**
> これは OCR / 翻訳エンジン選定の前提。

---

## 2. アーキテクチャ

```
[Capture]        Windows.Graphics.Capture
     ↓ GPU texture
[Preprocess]     クロップ / 2〜4倍拡大 / コントラスト補正 / 背景除去 / 差分判定
     ↓
[IOcrEngine]     PaddleOcrEngine（既定） / WindowsOcrEngine（高速モード・フォールバック）
     ↓ text + bbox
[TextAggregator] 行結合 / ノイズ除去 / キャッシュ判定
     ↓
[ITranslator]    LocalNmtTranslator（既定） / GeminiTranslator / OpenAiCompatibleTranslator
     ↓            └ 429・ネットワークエラー時は LocalNmtTranslator へ自動フォールバック
[NumberVerifier] 原文と訳文の数値を照合。不一致なら原文併記へ
     ↓
[Overlay]        レイヤードウィンドウ（透過・クリックスルー・最前面）
```

### 横断コンポーネント

| インターフェース | v1.0 の実装 | 将来 |
|---|---|---|
| `IInputTrigger` | `RawInputTrigger` / `GamepadTrigger` | ─ |
| `IEntitlementService` | `FreeOnlyEntitlementService`（常に無料版） | `SignedTokenEntitlementService` |
| `IAdProvider` | `NullAdProvider`（何も描画しない） | 契約した広告ネットワーク |
| `IRemoteConfig` | 署名付きJSONをCDNから取得 | ─ |
| `ProfileStore` | JSON ローカル永続化 | ─ |

**ONNX Runtime は PaddleOCR とローカルNMTで共有する。**

---

## 3. 機能要件

### 3.1 画面キャプチャ (FR-CAP)

| ID | 要件 |
|---|---|
| FR-CAP-01 | WGC でゲームウィンドウまたはモニタをキャプチャする |
| FR-CAP-02 | **キャプチャ方式を「ウィンドウ指定」「モニタ全体」から切替可能**（アンチチート更新で片方が塞がれた場合の保険） |
| FR-CAP-03 | 黒画面・空フレームが続く場合、検知してもう一方への切替をUIで提案する |
| FR-CAP-04 | 対象ウィンドウを選択し、プロセス名で次回以降自動再選択する |
| FR-CAP-05 | 翻訳対象領域（矩形）をマウスドラッグまたは数値入力で指定する |
| FR-CAP-06 | 領域は複数登録でき、プロファイルとして保存する |
| FR-CAP-07 | `IsBorderRequired = false` でキャプチャ枠を非表示にする |
| FR-CAP-08 | キャプチャ失敗時は原因（排他フルスクリーン等）と対処をUIで案内する |
| FR-CAP-09 | マルチモニタ / 高DPI / HDR 環境で座標がずれない |

### 3.2 OCR (FR-OCR)

| ID | 要件 |
|---|---|
| FR-OCR-01 | 指定領域から文字列とバウンディングボックスを取得する |
| FR-OCR-02 | 認識言語を指定できる（英・中・韓・日を初期対応） |
| FR-OCR-03 | **既定は PaddleOCR。設定で高速モード（Windows.Media.Ocr）に切替可能** |
| FR-OCR-04 | **OCR前に対象領域を拡大する（既定2倍、1〜4倍で設定可能）** |
| FR-OCR-05 | 前処理（拡大・二値化・コントラスト補正・背景除去・反転）を領域ごとにON/OFF |
| FR-OCR-06 | 信頼度が閾値未満の行を破棄できる |
| FR-OCR-07 | **直前フレームとの差分がなければOCRをスキップする** |
| FR-OCR-08 | モデルはアプリ起動時に非同期ロードする（初回OCRで待たせない） |
| FR-OCR-09 | PaddleOCR 初期化失敗時は Windows.Media.Ocr へ自動フォールバックし、UIに表示 |
| FR-OCR-10 | 高速モードで言語パック未導入なら `ms-settings:regionlanguage` への導線と手順を案内 |
| FR-OCR-11 | **推論スレッド数の上限を設定できる**（既定は物理コア数の半分程度） |
| FR-OCR-12 | GPU推論は既定で無効。有効化は明示的な設定とする |

### 3.3 翻訳 (FR-TRN)

| ID | 要件 |
|---|---|
| FR-TRN-01 | 複数の翻訳バックエンドを `ITranslator` で切替できる |
| FR-TRN-02 | **既定はローカルNMT。APIキーを登録しなくても全機能が動作する** |
| FR-TRN-03 | **ローカルNMTのモデルは言語ペア単位でオンデマンドDL**（進捗バー表示） |
| FR-TRN-04 | クラウド翻訳はユーザー自身のAPIキーを登録する（BYOK） |
| FR-TRN-05 | APIキーは DPAPI 等でローカル暗号化保存する |
| FR-TRN-06 | **429 / ネットワークエラー時は静かにローカルNMTへフォールバックして継続** |
| FR-TRN-07 | **クラウドLLMのモデル名は設定値で保持し `IRemoteConfig` で更新可能**（ハードコード禁止） |
| FR-TRN-08 | **原文の数値を抽出し訳文と照合。不一致なら警告または原文併記** |
| FR-TRN-09 | 同一原文の翻訳結果をキャッシュする |
| FR-TRN-10 | OCRの行分割で切れた文を結合してから翻訳する |
| FR-TRN-11 | 用語集を登録し翻訳に反映（LLM使用時はプロンプトに載せる） |
| FR-TRN-12 | 当月のAPI使用量と概算費用を表示する |
| FR-TRN-13 | 翻訳モードのバッジを小さく表示（**トグルで非表示可**） |
| FR-TRN-14 | キー登録画面で **Gemini 無料枠のプロンプトが製品改善に利用されうる旨**を通知 |
| FR-TRN-15 | OpenAI互換選択時、**ChatGPT Plus / Pro ではAPIを利用できない旨**を明記 |
| FR-TRN-16 | 「キーを取得する」ボタン → 発行ページを開く → 貼り付け → テスト翻訳で即時検証 |

### 3.4 オーバーレイ (FR-OVL)

| ID | 要件 |
|---|---|
| FR-OVL-01 | 常に最前面に透過ウィンドウで翻訳結果を表示する |
| FR-OVL-02 | **クリックスルー** |
| FR-OVL-03 | **ゲームウィンドウのフォーカスを奪わない** |
| FR-OVL-04 | 表示位置モード: ①原文位置に重ねる ②画面下部に字幕風 ③固定位置 |
| FR-OVL-05 | フォント・サイズ・文字色・背景色・不透明度・枠線を設定できる |
| FR-OVL-06 | 一定時間で自動フェードアウト（時間は設定可能） |
| FR-OVL-07 | 原文と訳文を併記/切替できる |
| FR-OVL-08 | オーバーレイ全体を1キーで即時ON/OFF |
| FR-OVL-09 | Alt+Tab の一覧に出ない |
| FR-OVL-10 | **処理時間の実測値を小さいオーバーレイで表示できる。既定は非表示、トグル可、小サイズ**（録画時の映り込み配慮） |

### 3.5 入力 (FR-INP)

| ID | 要件 |
|---|---|
| FR-INP-01 | ホットキーは全て変更可能。既定値は衝突しにくい組み合わせ |
| FR-INP-02 | **Raw Input で検知し、キー入力はゲームにそのまま流す** |
| FR-INP-03 | Windows.Gaming.Input でゲームパッドのボタン組み合わせを検知 |
| FR-INP-04 | **パッド入力は読み取りのみ。ブロック・書き換えを一切行わない** |
| FR-INP-05 | トリガー条件を「未使用ボタン単押し」「同時押し」「長押し(ms指定)」から選択 |
| FR-INP-06 | パッドだけで「翻訳実行 / 自動モード切替 / 表示ON-OFF」が完結する |
| FR-INP-07 | パッドの接続/切断に自動追従 |
| FR-INP-08 | 入力監視は低頻度ポーリング（8〜16ms程度） |

### 3.6 動作モード (FR-MOD)

| ID | 要件 |
|---|---|
| FR-MOD-01 | 手動モード: トリガー時に1回だけ翻訳 |
| FR-MOD-02 | 自動モード: 指定領域を監視し、変化検知で翻訳 |
| FR-MOD-03 | 監視間隔・変化検知の感度を設定できる |
| FR-MOD-04 | **1時間あたりの最大API呼び出し回数の上限を既定で有効にする** |
| FR-MOD-05 | ゲームが非フォアグラウンドなら監視を停止 |
| FR-MOD-06 | **PvPモード**: プロファイル単位で有効化。自動監視を**既定OFF**にする |
| FR-MOD-07 | **PvPモードでも自動監視はユーザーが手動でONにできる（強制しない）** |
| FR-MOD-08 | PvPモード有効時、初回に処理時間の実測表示を案内 |

### 3.7 設定・履歴 (FR-CFG / FR-LOG)

| ID | 要件 |
|---|---|
| FR-CFG-00 | 保存先は `%APPDATA%\Glosslay\`。名前空間・アセンブリ名も `Glosslay` |
| FR-CFG-01 | プロファイル（対象ゲーム・領域・言語・表示設定・前処理設定）を保存/読込 |
| FR-CFG-02 | プロセス名を検知してプロファイルを自動適用 |
| FR-CFG-03 | プロファイルをJSONでエクスポート/インポート（コミュニティ共有を想定） |
| FR-LOG-01 | 翻訳履歴（原文・訳文・時刻）を保存 |
| FR-LOG-02 | 履歴を検索・エクスポート（テキスト/CSV） |
| FR-LOG-03 | 上限件数で自動削除。スクリーンショット保存は既定OFF・ローカルのみ |

### 3.8 広告 (FR-ADS) — v1.0 では配線のみ

| ID | 要件 |
|---|---|
| FR-ADS-01 | 広告スロットは設定画面・履歴画面のみ。**ゲーム中のオーバーレイには絶対に出さない** |
| FR-ADS-02 | 表示可否は**署名付きリモート設定JSON**で制御。契約成立まで `enabled: false` |
| FR-ADS-03 | 契約成立後は**アプリを更新せず**リモート設定の切替だけで表示開始できる |
| FR-ADS-04 | 有効なサブスク契約者には一切表示しない（リモート設定より優先） |
| FR-ADS-05 | 読み込み失敗・オフライン時は枠ごと消える |
| FR-ADS-06 | リモート設定の取得失敗時は**広告なしにフォールバック**（安全側） |
| FR-ADS-07 | 広告が個人データを扱う場合、初回に同意UIを表示 |

**v1.0 での実装範囲**: 制御フローの配線と `NullAdProvider` のみ。広告SDKは組み込まない。

```
起動 / 定期
  ├→ IEntitlementService.Get()      → 有料か?
  │        └ 有料 → 広告完全無効（リモート設定を無視して終了）
  └→ IRemoteConfig.GetAdConfig()    → 署名付きJSONを取得・検証
           ├ enabled=false / 取得失敗 → NullAdProvider
           └ enabled=true            → 指定 provider を生成しスロットへ描画
```

### 3.9 ライセンス (FR-LIC) — v1.0 では配線のみ

| ID | 要件 |
|---|---|
| FR-LIC-01 | 無料版とサブスク版を**同一バイナリ**で提供。差はエンタイトルメントで制御 |
| FR-LIC-02 | ライセンスキーで認証（アカウント登録は必須にしない） |
| FR-LIC-03 | **サーバーの秘密鍵で署名したトークンを発行し、クライアントは公開鍵で検証** |
| FR-LIC-04 | オフライン猶予期間（7〜14日）を設ける |
| FR-LIC-05 | 猶予期間経過後は**機能停止ではなく無料版へ降格** |
| FR-LIC-06 | 同時アクティベーション台数に上限（3台程度）。ユーザー自身で解除可能 |
| FR-LIC-07 | 決済失敗時は即停止せずグレースピリオドを設ける |
| FR-LIC-08 | 解約後もローカル設定・プロファイル・履歴は消さない |

**無料 / サブスクの差（案・v1.1 で確定）**

| 機能 | 無料 | サブスク |
|---|---|---|
| 手動翻訳 / PaddleOCR / ローカルNMT / クラウド翻訳(BYOK) | ○ | ○ |
| 自動監視モード | 同時1領域 | 無制限 |
| プロファイル数 | 3件まで | 無制限 |
| 用語集 | 基本のみ | 無制限・共有 |
| 翻訳履歴 | 直近50件 | 無制限・検索 |
| 広告 | 表示（v1.2以降） | 非表示 |

> **PaddleOCR とローカルNMTは既定機能。有料化してはならない。**

### 3.10 その他 (FR-SYS)

| ID | 要件 |
|---|---|
| FR-SYS-01 | 更新チェックと自動アップデート（リモートJSONを参照） |
| FR-SYS-02 | タスクトレイ常駐、自動起動（任意） |
| FR-SYS-03 | 多言語UI（日本語・英語） |
| FR-SYS-04 | クラッシュログのローカル保存（送信は同意ベース） |
| FR-SYS-05 | 初回セットアップウィザード |
| FR-SYS-06 | **SmartScreen警告の対処手順**をヘルプと配布ページに用意 |
| FR-SYS-07 | **リリース成果物の SHA-256 ハッシュ**を配布ページと GitHub Releases に掲載 |
| FR-SYS-08 | **難読化・パッカーを使用しない** |
| FR-SYS-09 | ポータブル版（ZIP）をインストーラ版と併せて配布 |
| FR-SYS-10 | **エンタイトルメント判定は全て `IEntitlementService` 経由**。v1.0 は常に無料版 |

---

## 4. 非機能要件

| 分類 | 要件 |
|---|---|
| 性能 | 手動翻訳のトリガーから表示まで **1.5秒以内** |
| 性能 | 自動監視中のゲームFPS低下は **3%以内** |
| 性能 | アイドル時 CPU 1%未満、メモリ 300MB未満 |
| 性能 | ローカルNMTの翻訳速度は**毎秒2〜3文以上** |
| 互換性 | **Windows 11 のみ**。マルチモニタ・高DPI・可変リフレッシュレート対応 |
| 安全性 | ゲームプロセスへの読み書き・インジェクションを行わない |
| 安全性 | 管理者権限を必須にしない |
| プライバシー | **既定構成（ローカルOCR + ローカルNMT）では画面テキストが一切外部送信されない** |
| 配布 | インストーラは PaddleOCR モデル同梱で 200MB 程度まで許容。翻訳モデルは言語ペア単位（30〜45MB）でオンデマンドDL |
| 可用性 | 翻訳API・ライセンスサーバー障害時もクラッシュせず劣化動作する |
| 保守性 | OCR / 翻訳 / 広告 / 課金 を差し替え可能なインターフェースで分離 |

---

## 5. 実装前に確認が必要な事項

| # | 確認事項 | 理由 |
|---|---|---|
| 1 | PaddleOCR のコード / モデル重みのライセンス | 同梱配布するため |
| 2 | Opus-MT 等ローカルNMTモデルのライセンス | 再配布するため |
| 3 | Gemini の現行モデル名と無料枠のレート制限 | 世代交代が速い |
| 4 | PP-OCRv5 の多言語モデルで英・中・韓・日をカバーできるか | 配布サイズと実装量に直結 |
| 5 | .NET 10 が現行LTSであること | .NET 8/9 は 2026-11-10 サポート終了 |
