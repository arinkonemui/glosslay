# CLAUDE.md — Glosslay

**このプロジェクトの指示は `AGENTS.md` に集約しています。まず `AGENTS.md` を読んでください。**

内容を重複させると必ず食い違いが生じるため、ここには Claude Code 固有の補足のみ記載します。

---

## 必読ファイル（優先順）

1. **`RULES.md`** — 絶対的な制約と禁止事項。**作業前に必ず読む**
2. `AGENTS.md` — プロジェクト概要・技術スタック・進め方
3. `PLAN.md` — 現在のフェーズと次のタスク
4. `SPEC.md` — **実装仕様（リポジトリ内の正）**

---

## 要件定義書はリポジトリ外にあります

```
C:\Users\arink\source\99.Cloude資料\009_ゲーム翻訳アプリ\01.要件定義\glosslay-spec.md
```

約90KBの完全版。**リポジトリ外のため通常はアクセスできません。**
実装作業では `SPEC.md` を正として扱ってください。

### 要件定義書を読む必要が出たとき

以下のような場合は、**人間にアクセス許可を求めてください**（勝手に探しに行かない）。

- `SPEC.md` と `RULES.md` のどちらにも答えがない仕様上の疑問がある
- 「なぜこの技術を選んだのか」の根拠を確認したい
- 過去に却下された選択肢かどうかを確認したい

許可された場合も、**全文読み込みは避けてください。** Grep で該当箇所を絞るか、
Read の offset 指定で部分読みしてください。

**主要セクション**

| 章 | 内容 |
|---|---|
| §0〜§1 | 前提・目的・対象範囲・ユースケース |
| §3 | 確定した基盤技術 |
| §4.1〜§4.5 | 個別の設計判断（**なぜその技術を選んだか**の根拠） |
| §5 | 費用構造 |
| §6 | 機能要件（FR-xxx の詳細） |
| §7 | 非機能要件 |
| §8 | アーキテクチャ |
| §9 | **却下した選択肢と理由** |
| §10 | 契約・法務チェックリスト |
| §11 | リリース計画 |
| §12 | リスク一覧 |
| §13 | 決定状況サマリと残タスク |

---

## 技術の再提案は不要です

「この技術を使った方が良いのでは」と考えたときは、**まず `RULES.md` を確認してください。**

以下は**すべて理由を明記して却下済み**です。再提案しないでください。

| 却下済み | 理由（要約） |
|---|---|
| Electron / Tauri / Rust / C++ | ネイティブAPI群との相互運用で C#/.NET に優位がない |
| WinUI 3 / WPF+WebView2 ハイブリッド | 透過・クリックスルーの情報量と単一スタックの価値を優先 |
| Tesseract OCR | Windows.Media.Ocr に対する優位がなく、CJK精度は PaddleOCR に劣る |
| DeepL / Google Translate | APIキーを持っているゲーマーがほぼいない |
| DLLインジェクション / フック描画 | **アンチチートBANリスク。恒久的に非採用** |
| GDI BitBlt / DXGI Desktop Duplication | Windows 11 では WGC で足りる |
| RegisterHotKey / 低レベルキーボードフック | 入力を奪う / キーロガー誤検知 |
| .NET 8 / .NET 9 | 2026-11-10 サポート終了 |

詳細な却下理由は要件定義書 §9 にあります。

---

## 進捗の記録

タスクを完了したら `PLAN.md` のチェックボックスを更新してください。

---

## 仕様変更が発生したとき

**人間の承認なしに仕様を変更しないでください。**

承認された場合、リポジトリ内の `SPEC.md` を更新した上で、
**「リポジトリ外の要件定義書も更新が必要です」と人間に明示的に伝えてください。**
2つのファイルは自動同期されないため、伝え忘れると乖離します。
