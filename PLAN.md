# PLAN.md — Glosslay 実装計画

進捗はこのファイルのチェックボックスで管理する。**タスク完了時に必ず更新すること。**

---

## 現在地

**フェーズ: v0.1 PoC（進行中 — P0-1〜P0-6 はほぼ完了 / 次は P0-7 精度計測 ★ゲート）**

| | 状況 |
|---|---|
| P0-1〜P0-6 | 実装は通し（キャプチャ → 前処理 → OCR → 翻訳 → オーバーレイ、ホットキーで全画面翻訳）|
| 積み残し | 排他フルスクリーンの実測（P0-2）/ 中・韓・日の検証（P0-4）/ 文字サイズ 小中大（P0-6）|
| **P0-7** | **未着手。サンプルが 2 タイトル（5 本以上が必要）で、人間からの提供待ち** |

---

## v0.1 PoC — 「読めるか」の検証

> **このフェーズの目的は「動くアプリを作ること」ではなく、
> PaddleOCR で実ゲームのテキストが読めるかを検証すること。**
> ここが成立しなければ他の全ての設計が無意味になる。
> **UI の作り込み、設定画面、プロファイル、広告・課金の配線は対象外。**

### P0-1. 環境構築

- [x] .NET 10 (LTS) SDK の導入確認 — `10.0.400` を winget で導入済み
- [x] WPF プロジェクト作成（名前空間 `Glosslay`）— 出力は `Glosslay.exe`
- [x] ソリューション構成を決める（`Glosslay.App` / `Glosslay.Core` 程度の最小構成でよい）

```
Glosslay.slnx
├─ src/Glosslay.App   … WPF 本体。AssemblyName = Glosslay（配布 exe）
└─ src/Glosslay.Core  … UI 非依存の中核ロジック（Capture / OCR / 翻訳 / Overlay 相互運用）
```

- TargetFramework 等は `Directory.Build.props` で一元管理し、個別 csproj では上書きしない
- `src/Glosslay.App/app.manifest` で `asInvoker`（昇格しない）と `PerMonitorV2`（FR-CAP-09）を宣言
- WGC / `Windows.Media.Ocr` / `Windows.Gaming.Input` が現行 TFM から参照可能なことを確認済み

### P0-2. 画面キャプチャ

- [x] Windows.Graphics.Capture でウィンドウキャプチャを実装
- [x] `IsBorderRequired = false` でキャプチャ枠を消す — 実測で `IsBorderRemoved=True`
- [x] モニタ全体キャプチャも実装し、切替できるようにする — `CaptureTargetKind` で切替
- [x] 高DPI環境で座標がずれないことを確認 — 下表のとおり `app.manifest` の `PerMonitorV2` で解消
- [x] **ボーダーレスウィンドウ（Windowed Fullscreen）のゲームで取得できることを実測** — 2026-09-09
- [ ] 排他フルスクリーン（Exclusive Fullscreen）で取得できるか実測する — **未消化**

**実ゲームでの確認結果（2026-09-09）**

| 項目 | 結果 |
|---|---|
| 対象 | 実ゲーム（Windowed Fullscreen で動作） |
| 取得サイズ | 1920x1080・非黒 100% |
| 枠の除去 | 成功 |
| フレーム取得 | 15.6 ms |

> **ボーダーレスウィンドウが本アプリの対象モード。** ここが成立すれば、
> モニタ 1 枚でも全画面表示のままオーバーレイ翻訳ができる（利用者のゴール）。
>
> **排他フルスクリーンは方式上サポートできない。**
> 外部から描画するにはゲームプロセスへのフックが必要で、RULES.md 🔴-1 が恒久的に禁止している
> （アンチチートによる BAN リスク）。キャプチャ側も同じ制約を受ける可能性が高い。
> 対応方針は「ボーダーレスへの切替を UI で案内する」（FR-CAP-08 / FR-CAP-03 の延長）。

**実装（すべて `Glosslay.Core/Capture/`）**

| ファイル | 役割 |
|---|---|
| `CaptureTarget.cs` | 対象の識別（HWND / HMONITOR）。ウィンドウとモニタの切替（FR-CAP-02） |
| `CaptureTargetEnumerator.cs` | 対象候補の列挙。非表示・タイトルなし・DWM cloaked を除外 |
| `D3D11CaptureDevice.cs` | GPU テクスチャ → CPU 読み出し。ステージングテクスチャは使い回す |
| `ScreenCapture.cs` | WGC セッション本体。`GetNextFrameAsync` で 1 枚取得 |
| `CapturedFrame.cs` | BGRA32 の 1 フレーム |

**高DPI の実測結果（125% スケーリング環境）**

| | DPI 非対応 | DPI 対応（PerMonitorV2） |
|---|---|---|
| `GetMonitorInfo` の報告値 | 1536x864 ❌ ずれる | **1920x1080** ✅ 一致 |
| WGC の実キャプチャ | 1920x1080 | 1920x1080 |

> WGC は常に物理ピクセルを返すが、Win32 の座標 API は DPI 非対応プロセスでは仮想化された値を返す。
> `Glosslay.App/app.manifest` の `PerMonitorV2` 宣言が必須（FR-CAP-09）。

**実測値**: セッション開始 31ms（ウィンドウ）/ 289ms（モニタ初回）、初回フレーム取得 9〜23ms。

**判明した設計上の事実**

- Windows 11 では `GraphicsCaptureItem.TryCreateFromWindowId` / `TryCreateFromDisplayId` が射影済みで、
  **`IGraphicsCaptureItemInterop` の COM 相互運用は不要**
- 枠なしキャプチャは未パッケージアプリでも
  `GraphicsCaptureAccess.RequestAccessAsync(Borderless)` が `Allowed` を返す
- **D3D11 の COM オブジェクトはアパートメントをまたげない。**
  WPF の UI スレッドは STA、WGC の `FrameArrived` は MTA のプールスレッド。
  UI スレッドで D3D11 デバイスを作ると読み出し時に `E_NOINTERFACE` で失敗する。
  `ScreenCapture.StartAsync` は内部で `Task.Run` して構築を MTA へ寄せ、
  `D3D11CaptureDevice` は STA からの呼び出しを明示的に弾く。解放は STA からでも安全（実測）。

| アパートメントの組み合わせ | 結果 |
|---|---|
| STA で作成 → MTA から使用 | ❌ 失敗（修正前の WPF がこれ） |
| MTA で作成 → MTA から使用 | ✅ |
| MTA で作成 → STA から解放 | ✅ |

> **検証はコンソール（全部 MTA）で済ませてはいけない。** それでは この不具合を取り逃す。
> STA + `DispatcherSynchronizationContext` を用意して WPF と同じ条件で確認すること。

**依存追加**: `Microsoft.Windows.CsWin32` 0.3.333（2026-09-08 承認済み）。
D3D11 の COM 相互運用をビルド時生成するソースジェネレータ。`PrivateAssets=all` のため実行時 DLL の同梱はなく、配布サイズ・AV 誤検知への影響はない（RULES.md 🔵）。

**検証台**: `Glosslay.App` の `MainWindow` に最小限の実験用 UI を用意した（UI の作り込みは PoC 対象外のため機能は最小限）。
排他フルスクリーンの実測と、P0-7 の精度計測用サンプル採取に使う。

- 方式切替（ウィンドウ / モニタ全体）・枠除去・カーソル有無のトグル
- **5秒後にキャプチャ** — 押してからゲームへ切り替えることで排他フルスクリーンを検証できる
- **非黒ピクセル率の表示** — ほぼ黒なら取得失敗と判断する材料（FR-CAP-03）
- PNG 保存先は `%APPDATA%\Glosslay\captures\`（FR-CFG-00）

### P0-3. 前処理

- [x] 矩形領域のクロップ — プレビュー上のドラッグで指定できる（FR-CAP-05）
- [x] **2〜4倍の拡大** — 双線形 / 双三次（Catmull-Rom）を選べる
- [x] コントラスト補正 — 倍率指定と自動コントラスト（ヒストグラム伸張）
- [x] 背景除去（カードのイラスト背景対策）— Sauvola の適応的二値化
- [x] 前フレームとの差分判定 — `FrameDifference`（FR-OCR-07）
- [x] **ジャンル別に前処理の最適値を記録する** — 実ゲーム 2 画面で計測（2026-09-14）

**実装（`Glosslay.Core/Imaging/`）**

| ファイル | 役割 |
|---|---|
| `PreprocessOptions.cs` | 前処理の設定。プロファイルに紐づける想定 |
| `ImagePreprocessor.cs` | 拡大 → グレースケール → コントラスト → 二値化 → 反転 |
| `FrameDifference.cs` | 前フレームとの差分（FR-OCR-07） |

適用順を「拡大が先」にしているのは、二値化の判定に使える画素を増やしてから境界を決めるため。
先に二値化すると輪郭が階段状になり、後から拡大しても情報は戻らない。

画素単位の正しさは 15 項目の検証で確認済み（サイズ、RGB 一致、反転値、二値化の値域、
元画像を壊さないこと、差分判定の各ケース）。

**実ゲームでの計測結果（2026-09-14 / Pokémon TCG Live）**

計測対象は 2 画面。`tools/ocr-eval/OcrBench` で 11 通りを比較した。

| 画面 | 内容 | 該当 UC |
|---|---|---|
| `galvantula_ex` | 虹色ホロのカード詳細 1920x1080 | **UC-3（最難関）** |
| `learning_lab` | セリフ吹き出し + メニュー一覧 1920x1080 | UC-1 / UC-5 |
| `card_only` | 上記カード部分のみを切り出し 690x960 | UC-3（領域指定あり） |

**結論1: 最も効くのは「範囲を絞ること」。前処理ではない。**

| 条件 | 一致度 | 数値 | 時間 |
|---|---|---|---|
| カード・全画面 1920x1080 | 89.0% | 11/11 | 2119 ms |
| **カード・範囲を絞る 690x960** | **90.1%** | 9/9 | **1127 ms** |

範囲を絞るだけで**時間が約半分になり、精度もわずかに上がる**。
FR-CAP-05 の領域指定は、速度と精度の両方で最優先の機能。

**結論2: 拡大・コントラストは割に合わない。**

カード全画面での比較。

| 設定 | 一致度 | 数値 | 時間 |
|---|---|---|---|
| 拡大3倍 | 90.9% | 11/11 | 4682 ms |
| 拡大2倍 | 90.3% | 11/11 | 4489 ms |
| 拡大2倍+自動コントラスト | 90.3% | 11/11 | 4243 ms |
| **前処理なし** | 89.0% | 11/11 | **2119 ms** |
| 自動コントラストのみ | 89.0% | 11/11 | 1771 ms |

**+1〜2 ポイントのために時間が 2 倍以上**になる。既定では拡大しないこと。
領域を絞ってから拡大しても、`card_only` では 89.6〜90.2% で差が出なかった。

> PLAN の当初の想定「2〜4倍の拡大が最も効果が大きい」は、**PP-OCRv6 では成り立たなかった。**
> このモデルは短辺 736 未満の画像を内部で拡大するため、自前の拡大は二重処理になりやすい。

**結論3: 二値化と背景除去は明確に有害。既定で無効にすること。**

| 設定 | カード | メニュー | 数値の取りこぼし |
|---|---|---|---|
| 拡大2倍+背景除去 | 70.3% | 91.7% | 1 個 |
| 拡大2倍+二値化 | **52.5%** | 85.2% | 2 個 |

イラスト背景対策として入れたが、PP-OCRv6 は自然画像で学習されており、
二値化で落ちる情報のほうが大きい。**カードのような複雑な背景ほど悪化幅が大きい。**

**結論4: 数値はすべて正しく読めている。**

二値化・背景除去を除く全設定で **16/16 正解**。
カードの `260` `110+` `110` `180` `159/142`、メニューの `1/5` `0/11` `0/3` をすべて取れている。
RULES.md 🟡-7 が最重視する項目としては良好。

**推奨する既定値**

```
拡大 1.0 倍 / コントラスト 1.0 / 自動コントラスト OFF
二値化 OFF / 背景除去 OFF / 反転 OFF
→ 前処理より「領域を絞る」ことを優先する
```

> ⚠️ **この一致度は編集距離ベースの指標であり、P0-7 の 3 段階判定とは別物。**
> 合格判定には、ユースケースごとに 20 箇所以上を人が見て判定する必要がある。

---

**結論5: 速度問題はカーソル位置モード（FR-MOD-09）で解決できる。ただし窓の形が重要。**

| 条件 | 一致度 | 数値 | 時間 |
|---|---|---|---|
| 全画面 1920x1080 | 89.0% | 11/11 | 2119 ms |
| カード全体 690x960 | 90.1% | 9/9 | 1127 ms |
| **カーソル窓 640x480（正方形寄り）** | **99.6%** | **3/3** | **551 ms** |
| カーソル窓 500x300（幅が足りない） | 86.9% | **0/3** | 564 ms |
| カーソル窓 625x110（横長の帯） | 93.9% | 2/2 | 1328 ms |

> ⚠️ **訂正（2026-09-20）: 640x480 の「99.6%・数値 3/3」は、数値の分割を見逃していた。**
> 説明文の `this attack does 110 more damage.` を OCR が `1 10` と**数字の途中に空白を入れて**読んでおり、0.4% の減点はこれだった。
> 「数値」の列は正解の数値が認識結果に**部分一致**するかで数えており、同じカードの `110+` に一致したため見逃した。
> 計測ツールに「原文に無い数値（余分）」の列を足して判明。前処理 11 通りのうち、分割しなかったのは「拡大2倍+コントラスト1.6」だけ。
> **窓の形と速度についての結論は変わらない**が、この窓でも数値が完全に読めたわけではない（課題6 の関連）。

**要件の 1.5 秒に対して 551 ms。翻訳の時間を乗せても十分収まる。**

**⚠️ 検出コストは面積ではなく「縦横比」で決まる。**

PaddleOCR は**短辺**が 736 未満なら短辺が 736 になるまで拡大する。
そのため短辺が 736 未満の領域では、検出の入力画素数は **736² × 縦横比**になり、
元の面積とは無関係になる。

| 領域 | 実際の入力 | 画素数 | 縦横比 |
|---|---|---|---|
| 625x110（横長の帯） | 4192x736 | 3.09M | 5.68 |
| 1920x1080（全画面） | 1920x1088 | 2.09M | 1.78 |
| 500x300 | 1216x736 | 0.89M | 1.67 |
| **640x480** | 992x736 | **0.73M** | **1.33** |

**1 行だけを横長に切り出すのは最悪の選択。** 全画面より遅くなる。

**カーソル位置モードの設計指針（暫定・実装時に見直すこと → 課題3）**

1. **窓は正方形に近づける。** 縦横比 1.3 前後が最も安い。横長の帯にしない
2. **幅はテキストブロックを覆いきる大きさにする。** 足りないと**数値が切れる**
   （500x300 の窓では `110+` が `1`、`180` が `18` になり、数値の正解が 0/3 になった）
3. 既定は **640x480 程度**。551 ms / 一致度 99.6% の実績（**ただし `110` が `1 10` に分割されていた**。上の訂正を参照）
4. 検出したテキスト枠が窓の縁に接している場合は、窓を広げて再実行することを検討する

> **全画面より小さい窓のほうが精度も高い**（89.0% → 99.6%）。
> 余計な要素（上部バー、イラストだけの領域）が混ざらないため。

**計測時の注意（実際に踏んだ落とし穴）**

認識結果を **Y 座標だけで並べてはいけない。** 同じ行の要素が 1px のずれで入れ替わり、
文字列比較が壊れて「前処理が精度を下げた」という誤った結論が出た。
縦位置が近いものを行にまとめ、行内を X で並べること。
これは SPEC.md §2 の **TextAggregator が本来担う処理**であり、実装が必要。

**テスト UI に組み込み済み**: 拡大倍率・コントラスト・自動コントラスト・背景除去・二値化・反転を
切り替えられる。プレビュー上をドラッグすると、その範囲だけを OCR する。

### P0-4. OCR（★最重要）

- [x] **PaddleOCR の .NET 連携方式を決める → 案A（ONNX Runtime 直接実行）に決定（2026-09-09）**
  - [x] 案A: ONNX Runtime で直接実行 — **採用**
  - [x] 案B: 別プロセス化（Python/PaddlePaddle）— 不採用（速度・配布の両面で不利）
  - [x] 両方試して、実装容易性と速度で決定する

**決定理由: ONNX Runtime が Paddle ランタイムの約 8 倍速く、要件を満たす唯一の選択肢だった。**

| ランタイム | 領域 700x420 / 30行 | 要件 1.5s |
|---|---|---|
| **ONNX Runtime（案A）** | **約 0.43 s** | ✅ 満たす |
| PaddlePaddle（案B） | 2.66 s | ❌ 超過 |

内訳（ONNX / CPU 4スレッド）: 検出 165.1ms + 認識 30行を逐次 268.6ms。
※ 一括投入（バッチ）は 373.8ms でむしろ遅く、逐次でよい。

> **訂正（2026-09-09）**: 当初 0.33s と記録したが、検出の入力サイズを誤っていた。
> PaddleOCR は短辺が 736 未満なら**拡大**するため（`limit_type="min"`）、
> 700x420 は 1216x736 で推論される。672x416 で測った 58.2ms は過小評価だった。

**検出の入力サイズ（実測）**

| 元サイズ | 実際の入力 | 検出時間 |
|---|---|---|
| 400x300 | 992x736 | 129.7 ms |
| 700x420 | 1216x736 | 165.1 ms |
| 1920x1080 | 1920x1088 | 430.6 ms |

> **注意**: ONNX の数値は純粋な推論時間で、DB 後処理と CTC デコードを含まない。
> 実装後に再計測すること。ただし 6 倍の差は後処理のコストで埋まる規模ではない。

**P0-3 への申し送り**: PaddleOCR は短辺 736 未満の画像を内部で拡大する。
FR-OCR-04 の「OCR 前に 2〜4 倍拡大」と二重にリサンプルされる可能性があるため、
**前処理の拡大倍率は、この内部拡大を踏まえて決めること**（単純に掛け算しない）。
小さい領域ほど内部拡大の倍率が上がり、検出コストが元サイズに比例しない点にも注意。

**配布面でも案A が有利**: 案B は Python ランタイムの同梱が必要になり、
配布サイズと AV 誤検知の両方で不利（RULES.md の懸念に直撃する）。

**検証環境**: `tools/ocr-eval/.venv`（Python 3.13.7 / paddleocr 3.7.0 / paddlepaddle 3.3.1）。
venv と DL 済みモデルは .gitignore で除外。スクリプトのみコミットする。

**認識は成立している。** 自前 WGC キャプチャ（1920x1020）から 72 行を検出し、
日本語も正しく読めた（例: `ゲーム翻訳アプリ設定確認` score 0.987）。

**⚠️ 速度が要件から大きく外れている（PP-OCRv5 / CPU / oneDNN 無効）**

| 画像 | モデル | 推論時間 | 検出行数 |
|---|---|---|---|
| 領域 700x420 | mobile | **6.08 s** | 34 |
| 領域 700x420 | server | 27.85 s | 31 |
| 全画面 1920x1020 | mobile | 22.64 s | 66 |

> 要件は「トリガーから表示まで **1.5 秒以内**」（SPEC.md §4）。最良でも 4 倍超過。
> **ただし以下の交絡があり、この数値で PaddleOCR を判定してはいけない。**

**測定を歪めている要因**

1. **oneDNN（mkldnn）を無効にしている。** paddlepaddle 3.3.1 で有効にすると
   `NotImplementedError: ConvertPirAttribute2RuntimeAttribute not support` でクラッシュする。
   CPU 推論の主要な高速化手段を切った状態の数値。
2. **本命の案A（ONNX Runtime）が未計測。** 別ランタイムのため速度は別物になりうる。
3. 前処理（2〜4倍拡大）を未適用。適用すればさらに遅くなる方向。
4. 実運用は画面全体ではなく指定矩形（FR-CAP-05）。行数の少ない領域なら短くなる。

**ONNX モデルは公式配布がある。変換は不要だった。**
`PaddlePaddle/PP-OCRv6_small_det_onnx` / `_rec_onnx`（HuggingFace 公式・Apache 2.0）。
`paddle2onnx` での自前変換は、リリース版 paddlepaddle 3.3.1 と DLL のシンボルが合わず
（nightly 版が必要）断念したが、公式 ONNX があるため問題にならない。

**モデルサイズ（実測）**

| モデル | サイズ |
|---|---|
| **PP-OCRv6_small det / rec（ONNX・採用）** | 9.4MB / 20.2MB（計 **約30MB**） |
| PP-OCRv6_small det / rec（Paddle形式） | 9.7MB / 21MB |
| PP-OCRv5_mobile det / rec | 4.8MB / 17MB |
| PP-OCRv5_server det / rec | 85MB / 82MB |

配布サイズの制約（インストーラ 200MB 程度）に十分収まる。

**世代比較（Paddle ランタイム / 領域 700x420）**: v6 small = 2.66s に対し v5 mobile = 6.23s。
**PP-OCRv6 への切り替えは速度面でも正解だった**（約 2.3 倍速い）。

**認識モデルの出力クラス数は 18,710**（50 言語ぶんの文字辞書）。
- [x] PP-OCRv6 のモデルを組み込む — `PaddleOcrEngine`（ONNX Runtime）
- [ ] **多言語モデルで英・中・韓・日をどこまでカバーできるか検証** — 英は実証済み。中・韓・日は未実測
- [x] 文字列とバウンディングボックスを取得（FR-OCR-01）
- [x] 推論スレッド数を制限できるようにする（FR-OCR-11）

**C# 実装（`Glosslay.Core/Ocr/`）**

| ファイル | 役割 |
|---|---|
| `IOcrEngine.cs` | 差し替え可能な抽象（RULES.md の要求） |
| `PaddleOcrEngine.cs` | 検出 → 行の切り出し → 認識 → CTC デコード |
| `OcrImagePreprocessor.cs` | 入力テンソルの生成（リサイズ・正規化） |
| `DbPostProcessor.cs` | 確率マップ → 文字領域の矩形 |
| `CtcDecoder.cs` / `CharacterSet.cs` | 認識結果 → 文字列 |

**Python 版との突き合わせ（2026-09-09・同一モデル PP-OCRv6_small）**

検証画像（ゲームUI風・数値を含む 620x260）で **5 行すべてテキストが完全一致**。
数値（50 / 12 / 7.5 / 3 / 0/3）もすべて正しい。
信頼度はわずかに異なる（C# のほうが高め）が、これは双線形補間とボックス算出の実装差によるもの。

| 項目 | C#（ONNX） | Python（Paddle） |
|---|---|---|
| 認識テキスト | 5 行とも一致 | 基準 |
| 処理時間 | **556 ms** | 1009 ms |
| 初期化 | 356 ms | ─ |

**実装上の約束事はすべて本家の実装を読んで確定させた**（推測なし）。

| 項目 | 確定した内容 |
|---|---|
| チャンネル順 | **BGR**（yml の `img_mode: BGR` が既定の RGB を上書きする） |
| 検出の正規化 | `/255` → mean `[0.485,0.456,0.406]` / std `[0.229,0.224,0.225]` を **BGR の順**に適用 |
| 検出のリサイズ | 短辺 < 736 なら拡大、32 の倍数へ丸め、長辺上限 4000 |
| 認識の正規化 | `/255` → `-0.5` → `/0.5`（[-1,1]） |
| 認識の入力幅 | `max(320, 48 × アスペクト比)`、上限 3200、右側ゼロ埋め |
| CTC のクラス対応 | クラス 0 = blank、1〜18708 = 辞書、18709 = 空白文字 |

**本家との相違点（意図的な簡略化）**: DB 後処理で回転矩形ではなく連結成分の外接矩形を使っている。
対象が横書きのゲーム UI に限られるため。斜めのテキストが必要になったら作り直すこと。

### P0-5. 翻訳（PoC は Gemini のみ）

> PoC は自分用のため、APIキー登録の障壁が問題にならない。
> **ローカルNMTの実装は v1.0 まで持ち越す。**
> **「既定はローカルNMT」という順序（RULES.md 🟡-1）は変えていない。実装の順番だけが逆。**

- [x] Gemini API で翻訳する最小実装 — **2026-09-15 に実APIで疎通確認**
- [x] モデル名を設定値として保持（**ハードコードしない**）— `%APPDATA%\Glosslay\gemini.json`
- [x] プロンプトに「ゲームのスキル説明」「数値と固有名詞を変更しない」を明示

**実装**

| ファイル | 役割 |
|---|---|
| `Glosslay.Core/Translation/ITranslator.cs` | 差し替え点（RULES.md 🟡-2） |
| `Glosslay.Core/Translation/TranslationResult.cs` | 結果種別。**失敗理由を分けて持つ** |
| `Glosslay.Core/Translation/GeminiOptions.cs` | モデル名・プロンプト。JSON で外に出す（FR-TRN-07） |
| `Glosslay.Core/Translation/GeminiTranslator.cs` | `generateContent` の呼び出し |
| `Glosslay.Core/Configuration/ApiKeyStore.cs` | キーの暗号化保存（FR-TRN-05） |
| `Glosslay.Core/Configuration/DataProtection.cs` | DPAPI の P/Invoke（RULES.md 🔵 に従い集約） |
| `Glosslay.Core/Configuration/GlosslayPaths.cs` | `%APPDATA%\Glosslay\`（FR-CFG-00）の唯一の定義 |

**依存関係を増やしていない**（RULES.md 🔵）。DPAPI は
`System.Security.Cryptography.ProtectedData` パッケージでも使えるが、
既に承認済みの CsWin32 で `CryptProtectData` を生成する形にした。実行時 DLL は増えない。

**既定モデル: `gemini-3.5-flash-lite`**

当初は思考（thinking）が既定 OFF である `gemini-2.5-flash-lite` を選んだが、
**2026-09-15 に実機で 404 を踏んで差し替えた。**

```
404 NOT_FOUND: This model models/gemini-2.5-flash-lite is no longer available
to new users. Please update your code to use models/gemini-3.5-flash-lite
```

> ⚠️ **公式の deprecations ページは「終了日の告知なし」と書いていたが実態と違った。**
> 「既存利用者には残っているが新規プロジェクトからは使えない」という状態は、
> あのページに現れない。**モデルの可用性はドキュメントではなく実機で確かめること。**

`gemini-3.5-flash-lite` は思考を完全には切れないが、**既定が既に minimal** のため
`thinkingLevel` を指定しても変わらない。よって既定では送らない。
精度を上げたくなったら `"low"` 以上を試す余地として残してある。

**RULES.md 🟡-3 の例示について**（2026-09-15 に決着）

RULES.md は「Gemini 2.5 Flash-Lite は 2026-10-16 提供終了予定」と書いており、
私は公式ページと食い違うと報告したが、**実機の挙動は RULES.md 側が正しかった。**
そして「モデル名をハードコードしない」という原則そのものが、
**PoC の初回疎通で早速役に立った**（gemini.json を書き換えるだけで復旧できる状態だった）。

**失敗の扱い**（RULES.md 🟡-6 / FR-TRN-06）

| 状態 | フォールバック対象 | 理由 |
|---|---|---|
| 429 / 通信エラー / サービスエラー | **する** | 利用者の操作では直せない。止めない方が良い |
| キー未登録 | **しない** | 黙って隠すと「登録したのに効かない」ことに気づけない。UI で知らせる |

**検証済み**（`scratchpad/KeyStoreVerify`・17 項目すべて通過）

- DPAPI で暗号化保存 → 復号して一致する
- **保存したファイルに平文が入っていない**（FR-TRN-05 の実体）
- 貼り付け時の前後の空白・改行が落ちる
- キー未登録が `NotConfigured` になり、フォールバック対象にならない
- プロンプトに数値・固有名詞・ゲーム文脈・言語差し込み口が含まれる

**実APIでの確認結果（2026-09-15・pokemon.com の全画面キャプチャ）**

- [x] Gemini API との疎通
- [x] 応答 JSON の解析が実物と合っている
- [x] 訳文が返る。**処理時間 1849 ms**

| 観察 | 評価 |
|---|---|
| `30周年記念拡張パックの発売後、対象店舗を訪れて購入特典を受け取ろう` | 意味を保った訳。**数値 30 を保持** |
| `PoKeNbY` / `SléeP` / `POKeMaV` | ロゴの OCR 失敗断片。**訳さず原文のまま残した** |
| `CHAMPIONS` | 固有名詞として保持 |

後ろ 2 つはプロンプトの「意味が取れない断片は原文のまま残す」「固有名詞を変更しない」が
効いている証拠。**LLM が読めない箇所を創作で埋めていない。**RULES.md 🟡-7 の狙いどおり。

レイアウトは `scratchpad/OverlayVerify` で 1180x820 / 900x600 の両方を確認済み。
**前処理のグループを足したときと同じ崩れ方を繰り返さないため、UI に部品を足したら必ず走らせる。**

### P0-6. 最小オーバーレイ

- [x] レイヤードウィンドウを作成 — `Glosslay.App/OverlayWindow.xaml`
- [x] `WS_EX_LAYERED / TRANSPARENT / NOACTIVATE / TOOLWINDOW` + TOPMOST をすべて設定 — **実測で4つとも確認**
- [x] **クリックスルーが効くことをゲーム上で実測** — 2026-09-10 実ゲームで確認
- [x] **ゲームのフォーカスを奪わないことを実測** — 2026-09-10 実ゲームで確認

**実ゲームでの確認結果（2026-09-10）**

| 項目 | 結果 |
|---|---|
| クリックスルー | ✅ オーバーレイ上のクリックがゲームに通った |
| フォーカス維持 | ✅ 維持された |
| Alt+Tab | ✅ **オーバーレイは一覧に出ない**（出ていたのは操作ウィンドウで、これは正しい挙動） |
| FPS への影響 | **未計測**。FR-OVL-10 の処理時間表示を入れてから確認する |

> `WS_EX_TOOLWINDOW` が対象とするのはオーバーレイのみ。操作ウィンドウまで隠すと
> タスクバーからも消えてアプリを開けなくなるため、操作ウィンドウは Alt+Tab に出て正しい。
- [x] 訳文を表示する（体裁は最低限でよい）— 翻訳は P0-5 のため、現時点は**原文**を重ねる

**実装**

| ファイル | 役割 |
|---|---|
| `Glosslay.Core/Overlay/OverlayWindowStyles.cs` | 拡張スタイルの適用と**検証**（`Inspect`） |
| `Glosslay.Core/Capture/CaptureTargetBounds.cs` | 対象の画面上の矩形。DWM の見えている枠を優先する |
| `Glosslay.App/OverlayWindow.xaml` | 透過・最前面・クリックスルーの表示ウィンドウ |

WPF が用意できるのは `AllowsTransparency`（WS_EX_LAYERED）と Topmost まで。
クリックスルー・非アクティブ化・Alt+Tab 非表示は `SetWindowLongPtr` が必要。

**`WS_EX_TOOLWINDOW` の役割**
（FR-OVL-09 の根拠は「**ゲームの操作を妨げない**」に由来する。2026-09-09 に人間が確認済み）

`WS_EX_NOACTIVATE` は「自分から前面に出ない」ことしか保証せず、**Alt+Tab 経由で選択される経路が残る**。
選ばれるとゲームがフォアグラウンドを失い、ボーダーレスフルスクリーンのゲームは
FPS を落とす・音量を下げる実装が多い。「ゲームの操作を妨げない」（判断基準 2 位）が破れるため、
この経路を塞ぐのが `WS_EX_TOOLWINDOW`。副作用でタスクバーにも出なくなるため、
操作導線はタスクトレイ常駐（FR-SYS-02）で用意すること。

**ビルド構成の変更**: `Directory.Build.props` に `<PlatformTarget>x64</PlatformTarget>` を追加した。
`Platforms` だけではコンパイル対象が AnyCPU のままで、`SetWindowLongPtr` のような
ポインタ長依存の API を CsWin32 が生成できないため。AGENTS.md の「Windows 11 のみ（x64）」に沿う。

**未確認の点**: 認識画像と重ね先の座標対応は、ボーダーレスフルスクリーンなら 1:1 になる前提。
ウィンドウモードで枠の分だけずれる可能性があり、実ゲームでの確認が必要。

**指摘を受けて追加（2026-09-10）**: 実ゲームで「消去ボタンを押すまでオーバーレイが残り続ける」
「別アプリへ切り替えてもその上に残る」ことが判明した。前者は FR-OVL-06 の実装漏れ。

| 対応 | 内容 |
|---|---|
| FR-OVL-06 自動フェードアウト | 既定 10 秒で消える。実測で動作確認済み |
| 前面ウィンドウの追従 | ゲームか操作ウィンドウが前面の間だけ表示する。無関係なアプリへ切り替えたら即座に引っ込める |

前面の確認は `GetForegroundWindow` の問い合わせのみで、フックもプロセスへの干渉も行わない。
操作ウィンドウを許容リストに含めているのは、アプリを操作している最中に消えてしまわないため。
モニタ全体が対象のときはどれがゲームか特定できないため追従せず、自動フェードのみに任せる。

> **FR-OVL-06 の「時間は設定可能」は未実装**（現状は既定値のみ）。設定 UI は v0.5 の範囲。

### 前倒し: カーソルの周囲だけを翻訳（2026-09-22・人間の要望）

**なぜ前倒したか**: 課題3（窓サイズ）と課題4（1.5 秒に収まらない）は、
**この経路が無いと解けない。** 課題4 の対応方針が「カーソル位置モードを実装した段階で再計測する」
となっており、先に課題だけを片付けることができなかった。

**仕様への影響はない。**FR-MOD-09〜14 のうち、ホットキーで発動する経路だけを作った。

| 作った | 作っていない（v0.5 の本実装へ） |
|---|---|
| `GetCursorPos` で押した瞬間の座標を読む | **カーソル静止による発動**（FR-MOD-11。v0.5 ではこちらが既定） |
| 周囲 640x480 を切り出して既存の経路へ流す | 窓サイズ・静止時間の設定 UI |
| 既定 **Ctrl+Shift+C**（`hotkeys.json` で変更可能） | プロファイル・呼び出し上限・キャッシュ |
| 読んだ範囲と割合を計測の報告に出す | カーソルを使わないゲーム向けの無効化 |

- [x] `GetCursorPos` を CsWin32 に追加（**位置を問い合わせるだけ。ゲームプロセスには触れない**）
- [x] `CursorRegion` — 画面座標から画像座標へ直し、端では内側へ寄せる純粋な計算
- [x] 3 つ目のホットキーを配線（検出器は**全てに同じ入力を渡す**。渡さないと修飾キーの状態がずれる）
- [x] 座標計算の検証 — 21 項目すべて成功（等倍 / DPI 違い / 画像が窓より小さい / 対象の外 / 不正値）
- [ ] **実ゲームで動作確認と時間の計測** ← 次

**押す前にカーソルを読む。** 撮影に時間がかかるため、撮った後に読むと
「押した時に指していた場所」からずれる。

**見込み**（課題4 より）: 全画面 OCR 1648〜2119ms に対し、640x480 なら **551ms**。
訳す行数も激減するため、**1.5 秒に入るかどうかがここで分かる。**

---

### 前倒し: ホットキーで画面を翻訳（2026-09-17・人間の要望）

**要望**: スキルアイコンに触れると**別の場所**に説明が出て、カーソルを外すと消えるゲームがある。
カーソル位置モード（FR-MOD-09）では説明の場所を撮れないため、キーで画面全体を翻訳したい。

**仕様への影響はない。**既存の要件の組み合わせで、v0.5 予定の Raw Input ホットキーを前倒ししただけ。

| 使った要件 | 内容 |
|---|---|
| FR-MOD-01 | 手動モード: トリガー時に 1 回だけ翻訳 |
| FR-INP-01 | ホットキーは変更可能。既定値は衝突しにくい組み合わせ |
| FR-INP-02 | **Raw Input で検知し、キーはゲームにそのまま流す** |
| FR-OVL-04 ① | 原文位置に重ねる |
| UC-2 | スキル説明の想定トリガーが「手動トリガー」 |

- [x] Raw Input（`RIDEV_INPUTSINK`）でキーを受け取る — RULES.md 🔴-3 に従う
- [x] 既定 **Ctrl+Shift+T**。`%APPDATA%\Glosslay\hotkeys.json` で変更可能
- [x] **Ctrl+Shift+H** で訳文を消す / 出し直す（FR-OVL-08。2026-09-19 に追加。下の「F の対策」）
- [x] **押した瞬間の前面ウィンドウ**を撮る（一覧から選ぶ必要がない）
- [x] 行に番号を振って **1 回の呼び出し**で訳し、番号で元の位置へ戻す
- [x] 撮影直後にオーバーレイへ「翻訳中…」を出す（受け付けたことをゲーム画面上で返す）
- [x] **実ゲームで動作確認** — 2026-09-17 Tyr のテックツリー画面（下記）

**実ゲームでの確認結果（2026-09-17・Tyr・画面録画から計測）**

要望の場面そのもの（ノードに触れると**右上の別パネル**に説明が出る）で 3 回押した。

| 項目 | 結果 |
|---|---|
| 対応付け | **90/90 行**。実モデル（gemini-3.5-flash-lite）が番号の形式を 1 行も崩さなかった |
| 数値 | **すべて保持**。`0/5` `1.30` `-1.30` `50.00%` `25.00` |
| 撮影 | 約 290 ms。押してから 0.25〜0.35 秒で「翻訳中…」が出る |
| 最小化した操作ウィンドウ | 問題なし（Owner を外した修正が効いている） |

**所要時間（録画のコマ送りで計測）**

| 回 | 「翻訳中…」 | 訳文 | 撮影後の所要 |
|---|---|---|---|
| 1 | 3.75 秒 | 9.25 秒 | 5.5 秒 |
| 2 | 14.75 秒 | 20.75 秒 | 6.0 秒 |
| 3 | 26.25 秒 | 32.00 秒 | 5.75 秒 |

アプリの内訳表示（別の 1 回）: 撮影 290 / OCR 2516 / 翻訳 約 3250 / **合計 6055 ms**。
**要件 1.5 秒の約 4 倍**（課題4）。90 行という文字量が効いている。

**見つかった問題**

| # | 問題 | 実例 | 重さ |
|---|---|---|---|
| A | **訳文の枠が原文より狭いと、原文がはみ出して見える** | `アビリティコスト COST` / `クイックエレベーション ATION` / 訳文の横に `by 25.00.` が残る | 中。読みにくく、数値が 2 か所に見える |
| B | **数値は正しいが、用語を取り違えた** | `Elevate raises the turret 50.00% faster` → `砲塔の**旋回**速度が 50.00% 上昇`。昇降（仰角）を旋回（左右）と訳した | **高。**戦車ゲームでは別の性能値。流暢なので気付けない（RULES.md 🟡-7 と同種の害） |
| C | 訳す必要のない行まで重ねている | `0/5` の上に `0/5` を重ねる。テックツリーのノード番号など | 中。画面を無駄に覆う + 翻訳時間とトークンの浪費 |

B について: 3 回目では同じ「elevation」を `最大**仰角**時` と正しく訳している。**同じ語でも毎回同じ訳になる保証がない。**
FR-TRN-11（用語集）が本来の対策。

#### 問題 A への判断（2026-09-17・人間）

**原文がはみ出して見えること自体は問題にしない**（見えていても困らない）。

代わりに文字サイズの指定について要望があった。**優先度は低**（翻訳の所要時間に影響しないため）。

- [ ] オーバーレイの文字サイズを利用者が **小・中・大** から選べるようにする — FR-OVL-05「サイズを設定できる」の範囲内。仕様変更なし

**文字サイズの決まり方（現状）**: `OverlayWindow.CreateLabel` の 1 行。

```
文字サイズ = max(11, 原文の OCR 枠の高さ × 画面の倍率 × 0.62)
```

| 要素 | 意味 |
|---|---|
| 原文の OCR 枠の高さ | **ゲームが描いた原文と同じくらいの大きさ**で訳文を出すための基準 |
| × 0.62 | OCR の枠は文字の周りに余白を含む（検出の後処理で枠を 1.4 倍に広げている）。枠の高さのままだと原文より大きくなるため縮める |
| 最小 11 | 小さい UI 文字をそのまま写すと読めないため |

小・中・大は、この 0.62 に倍率を掛ければ実現できる（例: 0.8 / 1.0 / 1.3 倍）。
**描画は翻訳が終わった後の数ミリ秒の処理で、撮影・OCR・API のどれにも関与しない。所要時間には影響しない。**
ただし「大」はテックツリーのような密な画面で隣の訳文と重なりやすくなる（見た目の問題で、時間の問題ではない）。

#### 問題 C の修正（2026-09-17）

- [x] **訳す必要のない行を翻訳に回さない** — `LineTranslation.NeedsTranslation`
- [x] **訳して文字が変わった行だけを重ねる** — `LineTranslationResult.LinesToOverlay`

**除外のルールは実画面の行を分類してから決めた。**Tyr のテックツリー画面（録画の 2 秒時点）を OCR にかけた結果:

| 分類 | 行数 | 例 | 扱い |
|---|---|---|---|
| 文字を含まない | 38 | `0` `5` `-86` `62,778` | **送らない** |
| 英字 1 文字だけ | 11 | `T-85` `T-103`（段位）`Q` `Z`（キー表示） | **送らない** |
| 英字 2 文字以下の略号 | 4 | `LV11` `BV-1024` `日` | 送る |
| 語・文章 | 44 | `TECH TREE` `LOADOUT 1` | 送る |

3 段目を送る側に残したのは、4 行しか減らないのに `OK` `NEW` `MAX` のような訳す価値のある短い語を巻き込むため。
**漢字・かな等の 1 文字も送る**（中国語のゲームでは `火` 1 文字で意味を持つ）。**迷うものは送る側に倒す。**

**重ねる行の選別**: 翻訳に回した行でも、訳文が原文と同じなら重ねない（`X-04 < MRES` `ACCESS-LOG: 0029-I4` のようなコード類）。
大文字小文字と空白の違いは同じとみなす。

**効果（実画面・製品と同じコードで計測。API は呼んでいない）**

| 項目 | 修正前 | 修正後 |
|---|---|---|
| 翻訳に回す行 | 97 | **48（51% 減）** |
| 依頼文の文字数 | 1373 | **941（31% 減）** |
| 応答の行数 | 97 | **48** |

> **翻訳時間の短縮幅は未計測。**応答の行数が半分になるため縮む見込みだが、実 API で測って確かめること。

数値だけの行を送らないことは RULES.md 🟡-7 にも有利に働く。ゲームが描いた数値がそのまま見え、LLM が書き換える機会もない。

**検証**: 判定ロジック（`scratchpad/HotkeyVerify`）で、実画面に出た行（`0/5` `T-85` `日` `LV11` など）をそのままテストケースにした。
あわせて「依頼文の番号は 1 から詰めて振る」（飛び番にすると応答の解釈が壊れる）を確認している。

#### 問題 C 修正後の実ゲーム確認（2026-09-18・Tyr の RANGER 画面・画面録画から計測）

- [x] **ノードの `0/3` `0/5` と段位 `T-171` 等の上に枠が出なくなった**（録画のコマで確認）
- [x] 重ねたのは **106 行中 34 行**（翻訳に回したのは 55 行）。修正前は 90 行すべて重ねていた
- [x] 説明文の数値はすべて保持: `2.00` `+2.00` `2` `0.07` `-0.07`

**所要時間**

| 項目 | 修正前（DEADEYE 画面） | 修正後（RANGER 画面） |
|---|---|---|
| アプリの合計表示 | 6055 ms | **5018 ms** |
| 録画での撮影後の所要 | 5.5〜6.0 秒 | **5.0〜5.5 秒** |
| 検出した行 | 90 | 106（**多い**） |
| 翻訳に回した行 | 90 | 55 |

**短縮は翻訳側から来ている。**画面が違うため、両画面の OCR だけを同じ条件で 3 回ずつ測った:
DEADEYE 平均 2509 ms / RANGER 平均 2624 ms。**今回の画面の方が OCR は約 115 ms 遅い。**
それでも合計が約 1 秒縮んだので、翻訳はおおむね **3250 → 2100 ms 前後（約 35% 短縮）**と見積もれる。
（正確な値はアプリの結果欄の「翻訳」行。見積もりであることに注意）

> まだ要件 1.5 秒の約 3.3 倍。**残りの最大の塊は全画面 OCR（約 2.6 秒）**（課題4）。

**新たに見つかった問題**

| # | 問題 | 実例 | 重さ |
|---|---|---|---|
| D | **同じ用語が押すたびに違う訳になる**（問題 B の再現・悪化） | `Secondary Shells` が 2 回目は `サブ弾薬`、3 回目は `副砲`。3 回目の「`Requires Secondary Shells to be activated`」が `副砲の有効化が必要です` になり、**存在しない「副砲」というスキルを探させる** | **高。**スキル名の相互参照が壊れる |
| E | **行をまたぐ文で、後ろの断片が浮く** | `Generate 2.00 Energy whenever you land after being` / `airborne` → `着地するたびに2.00エネルギーを生成する` / `空中`。「空中から」の意味が 1 行目から抜けた | 中 |
| F | **前の訳文が、次のノードの説明の上に残る** | 11 秒時点: ゲームは SECONDARY SHELLS を表示しているのに、1 回目の `再生サスペンション` `着地するたびに2.00エネルギーを生成する` が重なったまま。**数値 2.00 がたまたま一致しており、取り違えやすい** | **高。**流暢で数値も合うため誤りに気付けない |

**D について**: 押すたびに独立した API 呼び出しで、前回どう訳したかを知らない。キャッシュ（FR-TRN-09）は原文が違うので効かない。
同じ呼び出しの中では揃う（見出しと説明文が同じ訳になる）ため、前の訳を伝えれば揃う見込み。

**D の対応方針（2026-09-20・人間の判断）: 手動の用語集（FR-TRN-11）だけで対応する。自動で覚える仕組みは作らない。**

| 自動で覚えない理由 | |
|---|---|
| 1 | 何を覚えるかの判断が、自動では難しい |
| 2 | 用語集のファイルが肥大化する |
| 3 | 用語集は無料からサブスクへの特典の候補（SPEC.md の「無料 / サブスクの差」で用語集は「無料: 基本のみ / サブスク: 無制限・共有」）。自動で増えると特典の価値が薄れ、登録件数の上限も決めにくくなる |
| 4 | 訳の揺れはそれほど大きな害になりにくい。**困ったら利用者に登録してもらえばよい** |

- 仕様（FR-TRN-11「用語集を登録し翻訳に反映」）のとおりなので、**仕様変更なし**
- 実装は予定どおり v1.0（下の「v1.0 公開」の「用語集」）。PoC では対応しない
- 揺れは当面そのまま受け入れる

**E について**: 依頼文に「行を結合・分割せず、**番号ごとに対応する部分だけ**を訳す」と書いていることが原因の可能性が高い。
2・3 回目では LLM がこの指示を緩めて 2 行へ意味を振り分け、上から自然に読める訳にしていた（`（1ポイントにつき` / `+2.00）`）。
**行数は守らせたまま、意味は行をまたいで振り分けてよい**と指示を変えれば改善する見込み。FR-TRN-10（行の結合）の軽量版にあたる。

**E の修正（2026-09-18）**

- [x] 依頼文の「番号ごとに**対応する部分だけ**を訳す」をやめ、**番号の数は守らせたまま意味は行をまたいで振り分けさせる**指示に変えた（`LineTranslation.BuildRequestText`）
- [x] 振り分けを許す代わりに崩してはいけないものを明記: 番号を増減させない / **どの番号も空にしない**（空は対応なし＝重ねないになる）/ **数値は必ずいずれかの番号に残す**（RULES.md 🟡-7）/ 見出しと説明文を 1 文にまとめない
- [x] 指示文の回帰テスト（古い指示に戻っていない・必要な制約が消えていない）
- [x] 実 API で旧・新の指示を比べる（説明パネル単位）— **E の再現に失敗。結論は出ていない**（下記）
- [x] **全画面の条件で旧・新を比べる** — 読み順の修正と合わせて **不具合 4/5 → 0/5**（下記「問題 E の本当の原因」）

**比べ方**: 前回の不具合は 3 回中 1 回しか出ておらず、**ゲームで 1 回試して直っても偶然と区別できない。**
録画から切り出した実画面（問題が出た REGENERATIVE SUSPENSION を含む 5 件）を OCR し、
**同じ入力に旧・新の指示を 2 回ずつ**当てて、訳文・対応数・数値の欠落を並べた（`scratchpad/PromptAB`、API 20 回・2026-09-18 人間の承認済み）。

**結果（説明パネル単位・3〜14 行ずつ）**

| ケース | 旧の指示 | 新の指示 |
|---|---|---|
| REGENERATIVE SUSPENSION（**問題 E が出た画面**） | 2/2 回とも**正しく振り分けた**（`空中から着地するたびに` / `エネルギーを 2.00 生成する`） | 2/2 回とも正しく振り分けた |
| SECONDARY SHELLS / RELOAD TIME / SIEGE / クロノクロス | 全回 対応 100%・数値欠落なし | 全回 対応 100%・数値欠落なし |

> ⚠️ **旧の指示でも不具合が再現しなかった。この比較では「新しい指示で直った」とは言えない。**

**分かったこと**

1. **新しい指示で悪くなったものはない。**20 回すべて対応 100%、数値の欠落 0
2. **不具合の条件が違う可能性が高い。**ゲームでは**全画面 55 行を 1 回で**送っていた。今回は説明パネルの**3 行だけ**。
   行数が多いと LLM が「対応する部分だけ」の指示を機械的に守りやすくなる、という仮説が立つ（未検証）
3. **翻訳時間は送る行数に強く依存する。**3〜14 行では 850〜1270 ms（全画面 55 行では約 2100 ms）。
   **カーソル位置モードのように範囲を絞れば 1 秒を切る**見込み（課題4 に効く材料）
#### 問題 E の本当の原因: 読み順で別の列の文字が文の途中に割り込んでいた（2026-09-18）

説明パネルだけで再現しなかったため、**全画面で送ったときの並び**を調べた。

| 番号 | 位置 | 文字 |
|---|---|---|
| [15] | x=1439 y=220 | `Generate 2.00 Energy whenever you land after being`（右の説明パネル） |
| **[16]** | **x=1167** y=249 | **`STN-1`**（左のテックツリーのノード名） |
| [17] | x=1439 y=244 | `airborne`（右の説明パネル） |

旧い読み順は「高さがほぼ同じなら同じ行とみなして左から並べる」だけだったため、
右パネルの `airborne` と左列の `STN-1` がたまたま同じ高さにあり、**文の途中に割り込んだ。**
LLM から見ると `airborne` は孤立した 1 語で、「空中」と訳すほかない。

**3 回の結果と割り込みが完全に対応している。**

| 画面 | 文の後半の直前 | ゲームでの訳 |
|---|---|---|
| REGENERATIVE SUSPENSION | **`STN-1` が割り込み** | **「空中」と浮いた** |
| SECONDARY SHELLS | 文の前半（割り込みなし） | 正しく振り分けた |
| RELOAD TIME | 文の前半（割り込みなし） | 正しく振り分けた |

→ **偶然ではなく、割り込みがあるときに起きていた。**依頼文の変更だけでは全画面で直らない。

**対策: 読み順を段落単位にした**（`Glosslay.Core/Ocr/TextAggregator.cs`。SPEC.md §2 の TextAggregator。FR-TRN-10 の土台）

1. 同じ高さで横の隙間が文字の高さの 2 倍以内の箱を、見た目の 1 行にまとめる（`It's fairy` / `sad,` を戻す）
2. すぐ下（隙間が高さ以内）で**横位置が重なる**行を段落にまとめる。**列が違えば重ならないので別段落**
3. 段落は崩さずに並べる。先頭行が同じ高さの段落は左から（上端だけで比べると、2px 上にある値 `30` がラベル `ENERGY COST:` より前に来るため）

`MainWindow` にあった旧い読み順は削除し、`TextAggregator` に置き換えた。

**効果（実画面 8 枚の OCR 結果・API は呼んでいない）**

| 項目 | 旧い読み順 | 新しい読み順 |
|---|---|---|
| 文の続きの行の直前に別の行が来た件数 | **34 / 69** | **15 / 69** |
| `Generate…` → `airborne` | 間に `STN-1` | **連続** |

残る 15 件は、画面下のボタンとフッター（`ESC` の下の `PROJECT TYR`）や、見出しの下の別項目など、
**文の続きではないもの**を判定の簡易ルールが拾ったもの。

> `tools/ocr-eval/OcrBench` は**旧い読み順のまま**にしてある。P0-7 の一致度が読み順の変更で変わると、
> 精度の変化と区別できなくなるため。切り替える場合は全サンプルの一致度を取り直すこと。

**検証**: 実画面の座標（`STN-1` と `airborne`、クロノクロスの `It's fairy` / `sad,`）、表のラベルと値、タブ列、2 段組みをテストケースにした（`scratchpad/HotkeyVerify`）。

**実 API での確認（全画面 55 行・ゲームで不具合が出た画面そのもの・API 10 回・2026-09-18 人間の承認済み）**

旧（旧い読み順 + 旧い指示）と新（新しい読み順 + 新しい指示）を、同じ画面に 5 回ずつ交互に送った（`scratchpad/PromptAB --fullscreen`）。

| 回 | 旧: `airborne` の訳 | 新: `airborne` の訳 |
|---|---|---|
| 1 | **`空中にいる`**（前の行で意味が完結し、浮いた断片が残る） | `2.00 エネルギーを生成する` |
| 2 | **`空中`** | `エネルギーを2.00生成する` |
| 3 | `2.00 エネルギーを生成する`（正しく振り分け） | `エネルギーを 2.00 生成する` |
| 4 | **`空中`** | `エネルギーを2.00生成する` |
| 5 | **`空中`** | `エネルギーを 2.00 生成する` |
| **不具合** | **5 回中 4 回** | **0 回** |

新は 5 回とも 1 行目が `空中から着地するたびに、` で、上から自然に読める。

| 項目 | 旧 | 新 |
|---|---|---|
| 行の対応 | 55/55（全回） | 55/55（全回） |
| 数値 | 22/22（全回） | 22/22（全回） |
| 翻訳時間 | 2202〜2781 ms | 2101〜2199 ms |
| 消費トークン | 約 1400 | 約 1495（指示文が長くなった分） |

**効いているのは主に読み順。**説明パネルだけ（割り込みなし）の比較では、旧い指示でも 2 回とも正しく振り分けていた。
指示文の変更は悪化がなく、振り分けの意図を明文化した保険として残す。

- [x] **全画面の条件で旧・新を比べる** — 不具合 4/5 → 0/5

4. **問題 D（用語の揺れ）は再現どころか想定以上。**`Secondary Shells` が 8 回の翻訳で
   `副砲` / `予備弾薬` / `セカンダリシェル` / `セカンダリーシェル` / `二次砲弾` / `二次弾薬`（ゲームでは `サブ弾薬`）と **7 通り**に揺れた。
   **同じ呼び出しの中では揃う**（見出しと説明文が同じ訳になる）が、呼び出しをまたぐと揃わない
5. 細かい品質: 1 回だけ `定点 siege モード` と英単語が訳されずに残った

**F について**: オーバーレイは押した瞬間の**写真**で、ゲーム画面が変わったことを知らない。押し直せば置き換わる（確認済み）。
仕様上の対策は FR-OVL-08（1 キーで即時 ON/OFF）と FR-OVL-06（消えるまでの時間を設定可能）。
「下の画面が変わったら自動で消す」は、**カーソルを外すと説明が消えるゲーム（要望の発端）で、読む前に訳文も消えてしまう**ため単純には採れない。

**F の対策: FR-OVL-08 を前倒し（2026-09-19）**

- [x] **Ctrl+Shift+H** でオーバーレイの表示を ON/OFF（`hotkeys.json` の `ToggleOverlay` で変更可能）
- [x] 実際のウィンドウ（STA）で動作を確認
- [x] **実ゲームで OFF（すぐ消える）を確認** — 2026-09-20 Tyr
- [ ] 実ゲームで ON（最後の訳文の出し直し）を確認 — 実際のウィンドウでは確認済み

| 操作 | 動き |
|---|---|
| 表示中に押す（OFF） | **すぐ消す**（フェード中も含む）← 問題 F への対策の本体 |
| 消えているときに押す（ON） | 最後の訳文を**出し直す。API は呼ばない**（読み返しに時間も料金もかからない） |
| 新しく Ctrl+Shift+T を押す | 前の訳文を**出し直しの対象から外す** |

**設計判断**

| 論点 | 判断 | 理由 |
|---|---|---|
| ON で何を出すか | 最後の訳文（`ShowLines` で出したもの）だけ | 「翻訳中…」などのお知らせを出し直すと、終わった処理が進行中に見える |
| 新しい翻訳を始めたら | 前の訳文を忘れる（`OverlayWindow.Clear`） | 覚えたままだと、翻訳中に ON/OFF を 2 回押しただけで**古い訳文が今の画面に戻る**（F と同じ害） |
| 翻訳中に OFF を押したら | 「翻訳中…」を消す。**訳文は出来上がれば表示する** | 翻訳は利用者が明示的に頼んだもの |
| 既定のキー | Ctrl+Shift+H（H = Hide） | 翻訳キーと修飾キーを揃えて覚えやすく。`Esc` はゲームのメニューも開くため不可（キーはゲームにも届く） |
| 2 つのキーが同じ組み合わせに設定されたら | ON/OFF 側を無効にして理由を表示 | 1 回押すたびに翻訳と ON/OFF が同時に走る |
| 検出器 | キーごとに 1 つ。**同じ入力を両方に必ず渡す** | 修飾キーの押下状態をそれぞれが持つため、片方にしか渡さないと状態がずれる |

> **FR-OVL-08 の解釈（PoC）**: いまは手動モードしかないため「表示中の訳文を消す / 最後の訳文を出し直す」とした。
> v0.5 で自動監視モード（FR-MOD-02）が入ると、「OFF の間は新しい訳文も出さない」という**全体スイッチ**としての意味が必要になる。そのときに決め直すこと。

**検証済み**

- 判定ロジック（`scratchpad/HotkeyVerify`）: Ctrl+Shift+T では翻訳だけ、Ctrl+Shift+H では ON/OFF だけが反応し、修飾キーの状態が両方で揃う
- 実際のウィンドウ（`scratchpad/OverlayVerify`）: 消す → 出し直すで同じ訳文が戻る / お知らせは出し直さない / **新しい翻訳の途中で 2 回押しても古い訳文は戻らない** / 出し直すのは最新の訳文 / 2 つのキーの説明が表示される

**未検証**: 同じ画面に 2 つのキーの定義が重なったとき（`hotkeys.json` を書き換える必要があり、利用者の設定を上書きしないよう試していない）。

**良かった点（想定外）**: 3 回目で OCR が 1 文を 2 行に割ったが、LLM は日本語の語順に合わせて
2 行へ意味を振り分け、上から読んで自然な文にした（`砲塔の最大仰角時に…ダメージを` / `25.00 増加させます。`）。

**実装**

| ファイル | 役割 |
|---|---|
| `Glosslay.Core/Input/RawKeyboard.cs` | Raw Input の登録と `WM_INPUT` の読み取り（P/Invoke を集約） |
| `Glosslay.Core/Input/HotkeyDetector.cs` | 押下の判定。**Win32 に依存しない**ため機械的に検証できる |
| `Glosslay.Core/Input/HotkeyGesture.cs` | `"Ctrl+Shift+T"` の解釈 |
| `Glosslay.Core/Input/HotkeyOptions.cs` | hotkeys.json |
| `Glosslay.Core/Translation/LineTranslation.cs` | 番号付きで訳し、位置へ戻す。v0.5 のカーソル位置モードでも使う |
| `Glosslay.Core/Configuration/JsonSettings.cs` | 設定 JSON の読み書きを共通化（日本語エスケープの不具合を再発させないため） |

**設計判断**

| 論点 | 判断 | 理由 |
|---|---|---|
| キーの記録 | **しない** | Raw Input はシステム全体の打鍵を受け取る。修飾キー 3 つの状態と 1 ビットしか持たない。残せばキーロガーと同じ |
| キーリピート | 押しっぱなしでは 1 回だけ | 連打扱いにすると API を払い続ける（RULES.md 🟡-8） |
| 修飾キー | **完全一致** | Ctrl+Shift+Alt+T では発動しない。ゲームの操作と重なる可能性を減らす |
| 実行中の再押下 | 無視 | 全画面は数秒かかる（課題4）。待ち行列を作ると同じ画面に何度も払う |
| 行ごとに API を呼ぶか | **呼ばない** | 行数倍の呼び出しになる。`[番号]` で並べて 1 回で訳す |
| 番号の形式を LLM が崩したら | 崩れた行だけ原文のまま、対応数を表示 | 黙って欠落させない。全角の `［１］` や `【1】` も受け付ける |
| 形式の指示をどこに書くか | **本文側** | システム指示は gemini.json で上書きできる。古い設定を持つ環境で番号が返らなくなる |
| プレビューの範囲指定 | **使わない** | 一覧で選んだ対象の画像に対して引いた範囲。前面の別ウィンドウに当てると見当違いの場所を切る |

**検証済み**

- ロジック 48 項目（`scratchpad/HotkeyVerify`）— 押下判定・キーリピート・修飾キーの完全一致・応答の解釈・位置への戻し
- **STA の実ウィンドウ上で Raw Input の登録が成功する**（`scratchpad/OverlayVerify`）
- レイアウト 1180x820 / 900x600

**未検証**: 実際の打鍵で `WM_INPUT` が届くか、LLM が番号の形式を守るか。**利用者の環境でキーを注入すると別アプリに入る恐れがあるため、自動では試していない。**

#### 見つけて直した不具合: オーバーレイが操作ウィンドウと一緒に最小化される

オーバーレイを操作ウィンドウの `Owner` にしていたため、**操作ウィンドウを最小化するとオーバーレイも最小化されていた。**
ボタン操作では操作ウィンドウを開いたまま押すため表に出なかったが、
ホットキーは「最小化してゲームをする」使い方で押されるので**訳文が黙って出なくなる。**

ドキュメントの記述を根拠にせず、実機で確かめてから直した。

| 条件 | 最小化後のオーバーレイ |
|---|---|
| Owner あり（修正前） | **見えなくなる** |
| Owner なし（修正後） | 見えたまま |

> **オーバーレイに `Owner` を付けないこと。**`MainWindow.EnsureOverlay` にも理由を書いてある。

#### 見つけて直した不具合: 対応付けの判定を推測していた

実装途中、「訳文が原文と同じなら対応が取れなかった」と推測する形で表示を作っていた。
**数値 `30` や固有名詞は、正しく訳しても原文と同じ文字列になる**ため、誤って「対応なし」と出る。
推測をやめ、行ごとの対応の有無を結果に持たせた。テストに `30 → 30` のケースを加えてある。

### P0-7. 精度計測 ★このフェーズの成果物

#### 計測方法（実装前に確定させる）

- [ ] 計測用のログ出力を実装（原文 / 信頼度 / 処理時間 / 使用した前処理設定）
- [ ] **各ユースケースにつき20箇所以上のテキストをサンプルとして採取**
- [ ] 正解テキストを手入力し、OCR結果と比較する
- [ ] 判定基準を以下の3段階で記録する

| 判定 | 定義 |
|---|---|
| ✅ 成功 | **意味が変わらず読める。数値が全て正しい** |
| ⚠️ 許容 | 誤字はあるが文意は取れる。**ただし数値が誤っていれば失敗扱い** |
| ❌ 失敗 | 文意が取れない、または**数値が誤っている** |

> **数値の誤りは、誤字より重く扱う。** スキル説明の「50ダメージ」が「60ダメージ」になるのは、
> 読めないより有害（`SPEC.md` UC-2 / `RULES.md` 🟡-7）。

#### 採取済みサンプル（2026-09-16 時点）

| サンプル | ゲーム | UC | サイズ | 一致度 | 数値 | 時間 |
|---|---|---|---|---|---|---|
| `galvantula_ex` / `card_only` / `hover_*` | Pokémon TCG Live | UC-3 | 各種 | 86〜99.6% | ⚠️ `110` が `1 10` に分割（2026-09-20 判明・課題6 の関連） | 386〜1052ms |
| `learning_lab` | Pokémon TCG Live | UC-5 | 1920x1080 | 93.6% | 5/5 | 1225ms |
| **`tyr_arbalest_skill`** | **Tyr** | **UC-2** | 710x490 | **100.0%** | **3/3** | **552ms** |

**`tyr_arbalest_skill` は実ゲームの UC-2 で満点。**スキル説明・数値（30 / 15s / 20s）・
ステータス名をすべて正しく読んだ。前処理なしが最速かつ最高精度で、**課題2 の結論を補強する**。

> ゲーム画面以外（Web サイト・Steam のようなランチャー）は計測対象に含めない。
> UI の描画方式が違い、ゲームの難しさを代表しないため。

#### 参考値: 文字表記のタイプ別（2026-09-16・`ref_` 接頭辞）

Web から拾った縮小画像のため**合否判定には使わない**（README の `ref_` の規約）。
ただし**高い結果は意味がある** — 低解像度で読めるなら実解像度ではもっと読める。

| サンプル | ゲーム | 文字の性質 | サイズ | 最良 | 設定 |
|---|---|---|---|---|---|
| `ref_chronocross_dialog` | クロノクロス | アンチエイリアス比例体 / **半透明枠越しに背景が透ける** | 340x123 | **100.0%** | 前処理なし |
| `ref_undertale_dialog` | アンダーテイル | **ピクセルフォント** / 白黒高コントラスト | 585x95 | **93.3%** | 拡大2倍+**反転** |

**どちらも文字そのものは完全に読めた。**

```
クロノクロス（信頼度 0.98〜1.00・誤り 0 文字）
  Razzly: / It's fairy / sad, / but I can understand / what he means.

アンダーテイル（信頼度 0.99〜1.00・誤りは先頭の * のみ）
  welp, that was a / long / break.
```

**結論1: 文字表記のタイプは障害にならない。**
半透明枠・ピクセルフォントとも、**320〜585px という極端に低い解像度でも**読めた。
実解像度ではこれ以上に読めるはずで、UC-1 は問題にならない見込み。

**結論2: 白地に黒でない文字は「反転」が効く。**
アンダーテイルは 83.3% → **93.3%**（反転あり）。白文字＋黒背景はモデルの学習分布と逆のため。
**ジャンル別の前処理最適値として記録すべき項目**（P0-7 の計測対象に既出）。
一方クロノクロスでは **背景除去が 61.0% まで落とす**。前処理は万能ではなく対象依存。

**結論3: 一致度の数字だけで合否を決めてはいけない。**
アンダーテイルの 93.3% の中身は「`*` が取れなかった」だけ。
`*` はセリフの装飾記号であって内容ではないため、**人の判定は ✅成功**になる。
数値の欠落なら同じ 93.3% でも ❌失敗。**P0-7 が 3 段階の人手判定を要求する理由がこれ。**

**副産物: 行の切れ方は正解と一致しない。**
`It's fairy sad,` が `It's fairy` / `sad,` の 2 箱に割れた。
一致度の計算では結合するため影響しないが、**このまま翻訳へ渡すと文が途中で切れる。**
FR-TRN-10（行分割で切れた文の結合）が必要である実測の裏付け。
現在の実装は全行を改行で繋いで 1 回の呼び出しにまとめているため、当面は破綻しない。

**全画面版を作らなかった理由**: 複雑なレイアウトでは正解テキストの並び順が
読み順の再構成と一致するとは限らず、**一致度が「並び順のずれ」に汚染される**。
2026-09-13 にこれで誤った結論を出している。全画面の傾向は `learning_lab` で足りる。

#### 参考値: UC-4 クエスト説明（2026-09-20・`ref_ptcgl_daily_quest`）

Pokémon TCG Live のバトルパス画面のデイリークエスト 3 件。利用者の**画面録画から切り出した**もので、
Glosslay のキャプチャ経路を通っていない（H.264 で圧縮されている）ため `ref_` とした。

| クエスト（原文） | OCR | 判定 |
|---|---|---|
| Evolve 1 Pokémon | `Evolve 1Pokémon`（空白が 1 つ欠けた） | ✅ |
| Attach 1 Tool to your Pokémon | 完全一致 | ✅ |
| Use 2 attacks from **⚡** Pokémon. | Use 2 attacks from **4** Pokémon. | ❌ **原文に無い数値** |

**⚡（雷エネルギーのアイコン）を数字の「4」と読んだ。**翻訳はそれを忠実に「**4 匹の**ポケモンのワザを 2 回使う」と訳した。

- 信頼度は **0.969**（他の 2 行は 0.988 / 0.989）。**OCR はほぼ確信して読んでおり、信頼度のしきい値（FR-OCR-06）では防げない**
- 前処理 11 通りのうち **10 通りで同じ誤り**。たまたまではなく、アイコンの形が「4」に見える
- 例外は二値化だけだが、他の部分の一致度が 76.1% まで落ちる
- **FR-TRN-08（原文と訳文の数値照合）でも防げない。**誤りは翻訳の前で起きており、原文（OCR の結果）にも訳文にも同じ「4」がある
- → 課題6 として起票

**計測ツールの見落としも見つかった。**OcrBench は「正解の数値を読めたか」しか数えておらず、この画像は **数値 6/6（満点）** に見えていた。
**原文に無い数値を「余分」として数える列を足した**（★ で表示。一致度に関わらず失敗扱い）。

**UC-4 としての不足**: デイリークエストは 1 行の短い文。SPEC.md の UC-4 は「クエスト説明・チュートリアル / **文章量が多い**」で、
**長い説明文のサンプルはまだ無い。**

**翻訳側の所見（参考）**: `Tool` を「グッズ」と訳した。ポケモンカードでは Tool は「ポケモンのどうぐ」、「グッズ」は Item で別物。
用語の誤りで、問題 D と同種（利用者の用語集で対応する方針）。

#### 計測対象

- [ ] **ジャンルの異なる実ゲーム5本以上** — 現在 **2 本**（Pokémon TCG Live / Tyr）
  - [ ] RPG・アドベンチャーのセリフ（UC-1）
  - [ ] スキル・アイテムの説明（UC-2）
  - [ ] **カードゲームのカードテキスト（UC-3 / 最難関）**
  - [ ] クエスト説明（UC-4）
  - [ ] メニュー・UI項目（UC-5）
- [ ] ジャンル別に前処理の最適値（拡大倍率・コントラスト・背景除去）を記録する

#### 合格基準

| ユースケース | 合格ライン（✅成功 の割合） |
|---|---|
| UC-1 セリフ / UC-4 クエスト説明 / UC-5 メニュー | **90% 以上** |
| UC-2 スキル説明 | **90% 以上**（数値の誤りは1件でも重く見る） |
| **UC-3 カードテキスト** | **70% 以上**（最難関のため基準を下げる。ただし前処理の調整で改善余地があること） |

- [ ] **判定: 上記を満たしたか**
  - 満たした → v0.5 へ進む
  - **UC-3 のみ未達** → 前処理の作り込みで改善を試みる。改善しなければ「カードゲームは限定対応」と仕様に明記して進む
  - **UC-1/UC-2 が未達** → **設計に戻る。** クラウドOCR や VLM の再検討を人間に提起する

> **この判定はプロジェクトの継続可否を決める最重要ゲート。**
> 曖昧なまま「なんとなく動いた」で次に進まないこと。

### P0-8. 並行タスク（開発以外）

- [ ] **arinkolab.com の開発ブログ更新を再開**（AdSense 審査に向けたコンテンツ蓄積）
- [x] PaddleOCR のライセンス確認（コード / モデル重みの両方）— 2026-09-08

**確認結果: コード・モデル重みとも Apache 2.0。同梱配布に問題なし。**

| 対象 | ライセンス | 根拠 |
|---|---|---|
| PaddleOCR 本体（コード） | Apache 2.0（追加条項なし） | リポジトリの LICENSE 全文を確認 |
| PP-OCRv5 モデル重み | Apache 2.0 | 公式 HuggingFace (`PaddlePaddle/PP-OCRv5_*`) のモデルカード |

条件は著作権表示とライセンス全文の同梱のみ。v1.0 の「OSSライセンス表記の同梱」で対応する。

### PoC 完了時に確定させること

- [ ] PaddleOCR の .NET 連携方式
- [ ] 前処理の既定値（拡大倍率など）
- [ ] 対応言語の初期セット
- [ ] ローカルNMTの具体的なモデル選定（Opus-MT系のどれか）

---

## v0.5 α — 実用最低限

- [ ] 自動監視モード（差分検知）— FR-MOD-02。利用者の言う「常に表示」はこれ
- [ ] **カーソル位置モード**（FR-MOD-09〜14・2026-09-10 追加）
  - [x] `GetCursorPos` でカーソル座標を取得（ゲームプロセスには触れない）— **2026-09-22 に前倒し**
  - [x] カーソル周辺の小領域だけを OCR する（全画面より検出が約 1/3 で済む）— **同上**
  - [ ] **発動条件の切替**（カーソル静止 / ホットキー押下）— 既定はカーソル静止。**ホットキー側のみ前倒し済み**
  - [ ] 静止判定の時間と読み取り範囲を設定可能にする
  - [ ] **呼び出し上限（FR-MOD-04）とキャッシュ（FR-TRN-09）を必ず通す**
  - [ ] カーソルを使わないゲーム向けに無効化できるようにする
- [ ] プロファイル（対象ゲーム・領域・言語・表示設定・**前処理設定**）
- [ ] プロセス名によるプロファイル自動適用
- [ ] Raw Input によるホットキー（**キーをゲームに流すこと**）— **画面翻訳の 1 本は PoC で前倒し済み**（2026-09-17）。残りは複数キーの割当と設定 UI
- [ ] Windows.Gaming.Input によるゲームパッド対応（**読み取りのみ**）
- [ ] トリガー方式3種（未使用ボタン単押し / 同時押し / 長押し）
- [ ] 設定UI
- [ ] **制限事項を小さく明記する**（2026-09-21 の要望）— 排他フルスクリーン非対応（課題1）と**アイコンに弱い**（課題6）。書く場所は 1 か所にまとめる
- [ ] **PvPモード**（自動監視を既定OFF・手動でON可能）
- [ ] 処理時間の実測表示（既定非表示・トグル可・小サイズ）
- [ ] Windows.Media.Ocr の高速モードとフォールバック
- [ ] 呼び出し回数上限・キャッシュ・使用量表示

---

## v1.0 公開

### 実装

- [ ] **ローカルNMT（Opus-MT系 int8 ONNX）を既定バックエンドとして実装**
- [ ] 言語ペア単位のモデルオンデマンドDL（進捗バー）
- [ ] クラウド翻訳へのアップグレード動線
- [ ] キー取得フロー（発行ページを開く → 貼り付け → テスト翻訳で検証 → 「後で設定する」）
- [ ] **429 / ネットワークエラー時のローカルNMTへの自動フォールバック**
- [ ] **数値検証**（原文と訳文の数値照合）
- [ ] 用語集
- [ ] 翻訳履歴（検索・エクスポート）
- [ ] 多言語UI（日本語・英語）
- [ ] 自動更新
- [ ] 初回セットアップウィザード
- [ ] **`IEntitlementService` の配線とゲート箇所を全て実装**（実装は常に無料版、上限は実質無制限）
- [ ] **`IAdProvider` / `IRemoteConfig` の配線**（`NullAdProvider` のみ）
- [ ] ライセンスキー入力UI（画面のみ作成、非表示または「準備中」）
- [ ] 設定ファイルにライセンス情報の保存領域を予約

### 配布

- [ ] インストーラ作成
- [ ] ポータブル版（ZIP）
- [ ] `arinkolab.com/apps/glosslay/` の配布ページ作成（**配布の主軸**）

#### GitHub 2リポジトリ構成のセットアップ

- [ ] `glosslay`（**Private**）— 本リポジトリ。仕様書は含めない
- [ ] `.gitignore` に念のため `01.要件定義/` と `*-spec.md` を追加しておく
- [ ] `glosslay-releases`（**Public**）を作成 — README と Releases のみ
- [ ] リリース用のビルド成果物を Public 側へ添付する手順を決める（手動 or CI）
- [ ] Public 側 README に配布ページ・SHA-256・VirusTotal 結果へのリンクを掲載

> **Private リポジトリでは Releases も認証必須になり、一般ユーザーが exe を落とせない。**
> 公開ダウンロードには Public リポジトリが必要。Public 側にはソースも仕様書も置かない。

### 未署名の離脱対策（§4.4 / 全て必須）

- [ ] **リリース頻度を絞る方針を決める**（未署名は**ビルドごとに信頼度がゼロにリセットされる**ため、こまめなリリースは逆効果）
- [ ] SmartScreen 警告のスクリーンショットと「詳細情報 → 実行」手順を配布ページに掲載
- [ ] インストール手順の動画/GIF
- [ ] SHA-256 ハッシュの公開（配布ページ + GitHub Releases）
- [ ] VirusTotal スキャン結果の先回り公開
- [ ] **難読化・パッカーを使わない**（AV誤検知を誘発するため）
- [ ] Microsoft Defender および主要AVベンダーへ誤検知報告を事前提出

### 法務

- [ ] EULA
- [ ] 既存のプライバシーポリシー・利用規約に**デスクトップアプリ配布とゲーム利用の条項を追記**
- [ ] 免責事項（**オンライン対戦での使用は各ゲームの規約次第でありBANの責任を負わない旨**）
- [ ] OSSライセンス表記の同梱
- [ ] **ローカルNMTモデルのライセンス確認**（再配布するため）
- [ ] プライバシーポリシーに**クラウド翻訳時の外部送信**と**Gemini無料枠の学習利用**を明記

### v1.0 リリース前に決めること

- [x] ~~OSS化するか否か~~ → **解消（2026-09-06）。OSS向け証明書は「非商用」が条件で本プロジェクトでは使えないため、OSS化の動機が消滅。コードは Private のまま**
- [ ] 無料版の上限値の具体的な数値
  - ⚠️ **無料版に上限を設ける予定の機能は、v1.0 の公開時点から上限を効かせること。**
    RULES.md 🟡-5「v1.0 で無料提供した機能を、後から有料側へ移さない」により、
    v1.0 を無料・無制限で出すと v1.1 以降に無料版を絞れなくなる。
    一方でこの計画の v1.0 は「常に無料版、上限は実質無制限」としており、**SPEC.md「無料 / サブスクの差（案）」の次の 4 つと食い違っている。**
    どちらに合わせるかを決めること（2026-09-20、問題 D の検討中に気付き、同日 4 つすべてが該当すると確認）

    | 機能 | SPEC.md の無料版（案） | v1.0 に入る予定 |
    |---|---|---|
    | 自動監視モード | 同時 1 領域 | v0.5 で実装 |
    | プロファイル数 | 3 件まで | v0.5 で実装 |
    | 用語集 | 基本のみ | v1.0 で実装 |
    | 翻訳履歴 | 直近 50 件・**検索なし** | v1.0 で実装（**検索・エクスポート付き**）← 表とそのまま衝突 |

    下の v1.1「⚠️ 未確定: v1.1 の価値提案」と同じ問題の別の面。あわせて決めること
- [ ] 配布ページの構成とコンテンツ計画

---

## v1.1

- [ ] ライセンス認証（署名付きトークン + ローカル検証 + オフライン猶予期間）
- [ ] サブスク決済（Stripe + Cloudflare Workers 無料枠）
- [ ] OpenAI互換エンドポイント対応（Ollama / LM Studio 向け）
- [ ] 収益が出ていればコード署名を導入（**Certum Standard / Individual・個人向け・クラウドHSM・年1.5〜2.3万円**）
  - 移行トリガー: 広告＋サブスクの月次収益が署名費用の3倍（月4〜6千円程度）を安定して超えた時点
- [ ] 特定商取引法に基づく表記 / インボイス対応 / サブスク利用規約

### ⚠️ 未確定

- [ ] **v1.1 の価値提案**（広告非表示は v1.2 以降のため訴求が弱い）
  - 案1: サブスク開始を v1.2 に合わせる
  - 案2: サポータープランとして出す
  - 案3: 有料機能を先に作る（前提Aと衝突）
- [ ] サブスクの価格帯

---

## v1.2 以降

- [ ] 広告ネットワークとの契約（デスクトップアプリ内掲載が許可されるものが見つかった場合）
- [ ] リモート設定でアプリ内広告を有効化
- [ ] （v2.0）プロファイルのコミュニティ共有
- [ ] （v2.0）カードDB照合による訳の高精度化

---

## 未解決の課題（仕様への反映が必要・文面と記載場所は未確定）

### 課題1: 排他フルスクリーン未対応であることの明記

**起票 2026-09-09。SPEC.md への反映は保留中。**

多くのゲームは既定で排他フルスクリーン起動し、しかも表示は単に「Fullscreen」であることが多い。
本アプリは方式上これに対応できない（外部から描画するにはゲームプロセスへのフックが必要で、
RULES.md 🔴-1 が BAN リスクのため恒久的に禁止）。**利用者が事前に知る手段が現状の仕様にない。**

現状の FR-CAP-08 は「キャプチャ失敗時に原因と対処を案内する」に留まり、次の 3 点が抜けている。

1. **失敗してから案内する**設計で、使う前に伝わらない
2. キャプチャの話しか書かれておらず、**オーバーレイも表示できない**ことに触れていない
3. 「Fullscreen」表記が排他を指す実態が反映されていない

**決定済み（2026-09-09・人間の判断）**

| 項目 | 決定 |
|---|---|
| 検知時の挙動 | **警告を出したうえで「それでも続行」を選べる**ようにする。環境によっては動く可能性が残るため、一律で止めない |

**未決定（要検討）**

- 明記する文面そのもの
- 記載場所の組み合わせ（SPEC.md の FR / 配布ページ / ヘルプ / 初回セットアップウィザード / アプリ内の警告）

> **課題6 の「アイコンに弱い」も同じ場所に書く**（2026-09-21 決定）。
> 制限事項を書く場所は 1 か所に決めて、両方をそこへまとめること。

**検知方法の候補**（いずれもゲームプロセスに干渉しない）

| 手段 | 内容 | 位置づけ |
|---|---|---|
| `SHQueryUserNotificationState` | `QUNS_RUNNING_D3D_FULL_SCREEN` が返るか見る | 主信号。Flip モデルでは返らないゲームもある |
| 黒フレームの継続検知 | FR-CAP-03 の仕組みを流用 | 補助。取りこぼしを拾う |

単独では取りこぼすため併用する想定。

---

### 課題2: FR-OCR-04 の「既定2倍」が実測と食い違う

**起票 2026-09-14。2026-09-22 に全サンプルで計測し直して整理。SPEC.md への反映は P0-7 の後。**

SPEC.md FR-OCR-04 は「**OCR前に対象領域を拡大する（既定2倍、1〜4倍で設定可能）**」と定めており、
`PreprocessOptions.Scale` の既定値 2.0 はこれに従っている。**仕様とコードは一致している。**
食い違っているのは実測結果のほう。

**計測（2026-09-22・サンプル 10 枚 / 4 タイトル・全サンプルで測り直した）**

| 拡大倍率 | 一致度（10 枚平均） | 時間（同） |
|---|---|---|
| **1 倍（拡大なし）** | **92.4%** | **691ms** |
| 1.5 倍 | 92.3% | 842ms |
| 2 倍 | 92.2% | 1104ms |
| 3 倍 | 91.5% | 1316ms |

**拡大するほど遅くなり、精度も上がらない。**
起票時の計測（1 ゲーム 2 画面。2 倍 90.3% / 4489ms に対し 1 倍 89.0% / 2119ms）と同じ向きで、
**規模を 10 枚に広げても結論は変わらなかった**（起票時は 1 倍のほうが 1.3 ポイント低かったが、平均では逆転している）。

**なぜそうなるか（仕組みは明確）**

PP-OCRv6 の検出は **短辺が 736 になるよう内部で拡大する**
（`limit_type="min"`・`OcrImagePreprocessor.DetectionLimitSideLength`）。
つまり **検出に渡る画素数は「短辺 736」に張り付いたまま、自前で拡大しても変わらない。**
効き始めるのは**拡大後の短辺が 736 を超えてから**で、それより下では二重のリサンプルになるだけ。

実測はこの通り 2 つの領域に分かれた。

| 短辺 | 2 倍にしたときの一致度 | 時間 | 件数 |
|---|---|---|---|
| **736 以上** | **+1.0pt** | **2.54 倍** | 2 |
| **736 未満** | **-0.5pt** | 1.13 倍 | 8 |

短辺 736 未満では、拡大しても**検出の入力サイズが変わらない**ため時間もほとんど増えず、
**二重リサンプルの分だけ精度が落ちる。** 最も小さい `hover_tight`（625x110）は **-3.5pt**。

> 裏取り: `hover_tight` は 1 倍でも 2 倍でも検出の入力が 4192x736 で**同一**（時間比 0.98 倍）。
> `card_only`（690x960）は 1 倍 736x1024 → 2 倍 1380x1920 で画素が 3.5 倍（時間比 1.45 倍）。

**FR-OCR-04 の前提が逆だった。** 「小さい UI 文字ほど拡大が効く」としていたが、
**小さい領域ほど内部拡大の倍率が大きく、自前の拡大が無駄になる。**

**残る不確かさ**

| 観点 | 評価 |
|---|---|
| 仕組みの説明 | **強い。** モデルの性質であり、ゲームに依存しない |
| 実測の裏付け | **中。** 10 枚 / 4 タイトルまで増えた。ただし正式サンプルは 2 タイトル（P0-7 は 5 本以上を求める） |
| 短辺 736 以上の裏付け | **弱い。** 1920x1080 の 2 枚だけ。**全画面翻訳はこの領域に入る** |

> 倍率の比較は**同じ画像の中での相対比較**のため、`ref_` サンプル（低解像度で実態より悪い数字が出る）の
> 影響を受けにくい。ゲーム本数の不足は、この論点に限れば軽い。

短辺 736 以上では 2 倍が +1.0pt を出しているが、**時間が 2.54 倍**になる。
課題4（全画面翻訳が 1.5 秒に収まらない）と、2026-09-21 の「速度に影響が出る案は避ける」判断に照らすと **採らない。**

**推奨（P0-7 の後に確定させる）**

| 項目 | 変更案 |
|---|---|
| FR-OCR-04 の既定 | **2 倍 → 1 倍（拡大なし）** |
| 1〜4 倍の設定自体 | **残す。** 領域ごとの調整（FR-OCR-05）として意味があり、短辺 736 以上では効く場面がある |

**今すぐ変えなくてよい理由**: `PreprocessOptions` を既定値のまま使っている箇所は現在ゼロ。
UI も計測ツールも倍率を明示的に渡しているため、**実際の動作はすでに拡大なし**。
変えても挙動は変わらない。

**⚠️ ただし放置すると v0.5 で踏む。**
自動監視やカーソル位置モードを実装するとき、仕様どおり 2 倍を既定にして処理時間を倍にしてしまう。

**対応方針**: P0-7 で 5 本以上のゲームを計測し、**仕様（SPEC.md）とコードを一度に変更する。**
要件定義書（リポジトリ外）の更新も同時に依頼する。

---

### 課題3: カーソル位置モードの窓サイズは実装時に見直す

**起票 2026-09-14。**

P0-3 の「結論5」に記録した窓サイズの指針（正方形寄り・640x480 程度・縦横比 1.3 前後）は、
**1 枚のカード画像から導いた暫定値**。

**確からしい部分**

- 検出コストが面積ではなく**縦横比**で決まること（仕組みから説明でき、ゲームに依存しない）
- 横長の帯が最悪であること
- 幅が足りないと**数値が切れる**こと（`110+` → `1` の実例）

**実装時に決め直すべき部分**

- 具体的な既定サイズ（640x480 は 1 例にすぎない）
- ゲームやジャンルごとの調整の要否
- 検出枠が窓の縁に接したときに窓を広げて再実行するかどうか

カーソル位置モードの形が見えてきた段階で、実データを取り直して決めること。

### 課題4: 全画面翻訳が性能要件（1.5秒）に収まらない

**起票 2026-09-15。実APIでの初回疎通で判明。**

SPEC.md の非機能要件は「**手動翻訳のトリガーから表示まで 1.5 秒以内**」。
pokemon.com の全画面キャプチャで実測すると、**翻訳だけで 1849 ms** かかり、
OCR を足すと 3.5〜4 秒になる。

| 工程 | 実測 | 出典 |
|---|---|---|
| OCR（全画面 1920x1080） | 1648〜2119 ms | P0-3 の計測 |
| 翻訳（全画面 30 行程度） | **1849 ms** | 2026-09-15 |
| 合計 | **約 3.5〜4 秒** | 要件の 2 倍以上 |

**ただし「全画面を一度に訳す」は本来の使い方ではない。**

| 経路 | OCR | 見込み |
|---|---|---|
| 全画面 | 1648〜2119 ms | **収まらない** |
| カーソル位置モード（640x480） | **551 ms** | 訳す分量も激減するため**収まる可能性がある** |

カーソル位置モード（FR-MOD-09〜14 / v0.5）と領域指定（FR-CAP-05）が、
そのまま性能要件への対策になっている。**設計は既に正しい方向を向いている。**

**追加の実測（2026-09-17・ホットキーで Tyr のテックツリー画面・90 行）**

撮影 290 / OCR 2516 / 翻訳 約 3250 / **合計 6055 ms**。録画のコマ送りでも 3 回とも撮影後 5.5〜6.0 秒。
**90 行のうち相当数が `0/5` のような数値だけの行**で、訳す必要がない（ホットキーの節の問題 C）。
→ **2026-09-17 に修正。**翻訳に回す行が 97 → 48、依頼文が 1373 → 941 文字。**翻訳時間の実測は未了。**

**まだ測っていないこと**

- カーソル位置モード相当の少量テキストでの翻訳時間（**今回測ったのは最悪ケースのみ**）
- 文字数と応答時間の関係
- `thinkingLevel` を上げ下げしたときの差

**対応方針**: v0.5 でカーソル位置モードを実装した段階で再計測する。
そこで収まらなければ、SPEC.md の 1.5 秒を見直すか、翻訳の非同期表示
（原文を先に出して訳文を後から差し替える）を検討する。**現時点では仕様を変更しない。**

> ⚠️ **1 件の計測**であり、しかも最悪ケース。この数字だけで要件を緩めないこと。

### 課題5: 利用者へのお知らせを表示できるようにする（要望・優先度低）

**起票 2026-09-17。人間の要望。優先度は低く、後回しでよいと指示あり。SPEC.md 未反映。**

**要望**: モデルの提供終了や新規利用者の受付停止は今後も起きる。
今回は既定モデルの変更で済んだが、**APIキーの再取得・再登録を利用者に頼む**場面もありうる。
そうした知らせを表示できるようにしたい。ツールの更新の知らせにも使う。

**既存の仕様との関係**

| 既存 | 内容 | 足りない点 |
|---|---|---|
| FR-SYS-01 | 更新チェック（リモート JSON を参照） | 「更新がある」以外の知らせを出せない |
| `IRemoteConfig` | 推奨モデル名の遠隔更新（RULES.md 🟡-2） | 利用者に**行動を求める**文面を持たない |
| FR-TRN-07 | モデル名を設定値として保持 | 同上 |

→ **お知らせの表示そのものは未定義。仕様の追加になる。**取り込む際は SPEC.md に FR を足し、
リポジトリ外の要件定義書にも反映が必要。取得は FR-SYS-01 のリモート JSON と同じ経路にまとめるのが自然。

**実装時に守るべき制約（先に書いておく）**

| 制約 | 理由 |
|---|---|
| ⚠️ **お知らせに外部リンクで「キーを入力する場所」を載せない** | **フィッシングの経路になる。**配信元や JSON が乗っ取られると「キーを再登録してください → 偽サイト」で利用者のキーを抜かれる。再登録は**必ずアプリ内の登録画面へ誘導**する |
| **配信する JSON に署名を付け、クライアントで検証する** | 上と同じ理由。RULES.md 🔴-2 のライセンス保護が「サーバー側の署名・公開鍵をクライアントに置く方式」で担保済みのため、同じ仕組みを流用できる |
| **ゲーム中のオーバーレイに出さない** | 判断基準の 2 位「ゲームの操作・描画を妨げない」。操作ウィンドウ / タスクトレイ（FR-SYS-02）に出す |
| **取得に失敗したら「知らせなし」として扱う** | RULES.md 🟡-6「リモート設定の取得失敗 → 広告なしとして扱う」と同じく安全側に倒す |
| **利用者の情報を送らない** | 取得は GET のみ。RULES.md 🔵「送信は明示的な同意ベース」 |

> 1 行目が最重要。「APIキーの再取得をお願いする」という要望の用途そのものが、
> 攻撃者にとって最も都合のよい文面になる。

### 課題6: 文中のアイコンが数字として読まれ、原文に無い数値が生まれる

**起票 2026-09-20。P0-7 の計測中に発見（`ref_ptcgl_daily_quest`）。**

Pokémon TCG Live の「Use 2 attacks from **⚡** Pokémon.」で、雷エネルギーのアイコン ⚡ が数字の **4** と読まれ、
「**4 匹の**ポケモンのワザを 2 回使う」と訳された。**原文に無い数値**で、RULES.md 🟡-7 が最も警戒する種類の誤り。

**どの仕組みでも止まらない**

| 仕組み | 止まらない理由 |
|---|---|
| OCR の信頼度（FR-OCR-06） | 0.969。OCR はほぼ確信している。**文字ごとに見ても止まらない**（下記の計測） |
| 前処理（FR-OCR-04/05） | 11 通り中 10 通りで同じ誤り |
| 数値照合（FR-TRN-08） | 誤りは翻訳の前。原文（OCR 結果）と訳文の両方に「4」があるため、照合は一致してしまう |

**影響範囲**: ポケモンカードは**エネルギーの記号を文中に頻繁に使う**（ワザのコスト、「⚡エネルギーを 1 個トラッシュ」など）。
UC-3（カードテキスト・最難関）と UC-4 の両方に効く。他のカードゲームや RPG でも、属性アイコンを文中に置く作りは珍しくない。

**決定済み（2026-09-21・人間の判断）— アイコンの翻訳は諦める**

| 案 | 決定 |
|---|---|
| 文字ごとの確信度で弾く（案A） | **否決。** 計測の結果、確率の分布が重なり分離できない（下記） |
| ゲーム別のアイコン認識（案B） | **不採用。** 「アイコンを絶対に誤読させないのは難しい。**他のゲームでも似た事象はあり得る**」ため、ゲームごとに潰す方針は取らない |
| 2 つ目の OCR（Windows.Media.Ocr）と突き合わせ | **不採用。** 「**翻訳の速度に影響が出るのはできる限り避けたい**」。OCR を 2 回通す時間は課題4 に直接乗る |
| 利用者への明記 | **採用。アイコンに弱いことを小さく明記する。** 文面と場所は UI を整える段階（v0.5 α の設定 UI）で決める。課題1 の「記載場所」と同じ場所にまとめる |

**この決定の意味**: ⚡ のようなアイコンは、今後も数字や記号として読まれることがある。
**アプリ側では止めない。**利用者が「アイコンは苦手」と知っていることを前提にする。

> 速度を優先する判断は、課題4（全画面翻訳が 1.5 秒に収まらない）にも同じ向きで効く。
> **精度のために OCR や翻訳の回数を増やす案は、以後この基準で判断する。**

原文併記（FR-OVL-07）は誤りを防がないが気付く手段にはなる。こちらは元から別枠で残る。

#### 案A（文字ごとの確信度）は計測の結果おとした — 2026-09-21

**きっかけ**: 誤読した「4」1 文字の確率は **0.503** で、行の平均 0.969 に埋もれていた。
`CtcDecoder` は採用した文字の平均だけを返すため、1 文字だけ自信がない状態が見えていなかった。
最初に調べた 3 行では、2 件の不具合がどちらも**行の中で最も低い文字**として現れていた。

**計測**: 全サンプル 10 枚 x 前処理 5 通り = **13,878 文字**の確率を集めた。結果、**分布が重なる**。

| | 確率 |
|---|---|
| 正常な文字（空白・`E`・`G`・`p` など） | **0.385** まで下がる |
| 誤読した「4」 | 前処理次第で **0.940** まで上がる（前処理なし 0.622 / 拡大2倍 0.875 / 拡大3倍 0.940） |

**既定の前処理（拡大2倍・FR-OCR-04）で 0.875。** この値は正常な数字（`110` の `1` が 0.700〜0.941 など）に埋もれる。

「文字が無い確率（CTC の blank）」も試したが同じ。誤読した「4」が 0.468 に対し、
**正しく読めた `x200` の `2` が 0.463** で、ここでも重なる。

**結論**: モデルが 0.875 で「4」だと言っているとき、**その出力のどこにも誤りの情報が無い。**
確率から導く指標（blank・上位 2 位との差・エントロピー）は、どれも同じ理由で分離できない。

行単位でしきい値を引いて原文併記に回す案も、0.70 で **19.4%（841 行中 163 行）** が該当し、量が多すぎる。

**残したもの**: `OcrLine.Characters`（文字ごとの確率）と、OcrBench の「最小」列。
判定には使えないが、**人が結果を読むときにどこが怪しいかが分かる**ため計測用に残す。

**副産物**: `拡大2倍+二値化` だけは ⚡ を `~` と読み、数値を生まなかった（余分 0）。
ただしこの設定は一致度が 76.1%（他は 87〜90%）で、全体では使えない。

**P0-7 の判定への影響**: 数値の誤りは 1 件でも重く見る基準（UC-2）。**UC-3 の合格ライン（70%）を割る要因になりうる。**
正式なキャプチャ（`ref_` でない）でも同じ誤りが出るかを確かめること。アイコンの形が原因なので出る見込みが高い。

#### 関連: 数字の途中に空白が入る（同日、計測ツールの修正で判明）

> **これは上の「アイコンを諦める」決定の対象外。** アイコンではなく、実在する数字の読み違いのため、未解決のまま残す。

ガルバンチュラ ex のワザの説明 `this attack does 110 more damage.` を、OCR が **`1 10`** と読んでいた。

| サンプル | 前処理 11 通りのうち分割した数 |
|---|---|
| `hover_square`（640x480） | 10（分割しなかったのは「拡大2倍+コントラスト1.6」だけ） |
| `card_only` | 9 |
| `galvantula_ex` | 7 |
| `hover_box500` | 7 |

**4 枚すべてで、ほとんどの設定で起きる**。同じカードの見出しの `110+` は正しく読めている（字間の広い書体で、文中の `110` だけ割れる）。

- 翻訳には `1 10` が渡る。LLM が「110」「10」「1〜10」のどれと解釈するかは保証がない
- 従来の「数値」の列は、`110` が `110+` に**部分一致**して「読めた」と数えていた。**2026-09-13 以降のガルバンチュラの数値の結果はこれを見逃している**
- 対応案（未決定）: 数字どうしの間の狭い空白を詰める後処理。ただし本当に別々の数値（`1 10`）を 1 つにしてしまう危険があるため、条件を絞る必要がある

**計測ツール側で見つかった書き漏らし**: `galvantula_ex` の正解テキストに、画面右上のコイン数 `0` が入っていなかった（上部メニューの行の右端）。
余分な数値として検出されたことで気付き、2026-09-20 に追加した。**OCR の誤りではなく、正解テキスト側の誤り。**

---

## 主要リスク（実装中に意識すること）

| リスク | 対応 |
|---|---|
| **OCR精度が出ない** | **PoC で判定する。ここが最大の関門** |
| カードのイラスト背景で認識率が落ちる | 拡大・コントラスト補正・背景除去を領域ごとに調整可能にする |
| ジャンルごとに最適な前処理が違う | 前処理をプロファイルに紐づけ、エクスポート/インポートで共有可能にする |
| 数値の誤読・書き換え | OCR側は信頼度評価、翻訳側は数値照合。原文併記の導線を常に残す |
| PaddleOCR の .NET 統合が重い | ONNX直接実行と別プロセス化の両方を PoC で試す |
| ローカル翻訳の品質で第一印象を損なう | モードバッジで水準を明示し、「精度を上げる」導線を見える位置に置く |
| アンチチート更新でキャプチャが停止 | ウィンドウ/モニタ全体の切替を用意（BANではなく機能停止のリスク） |
| PvPでのCPU競合 | 自動監視を既定OFF、スレッド数制限。**強制はせず実測値で判断させる** |
