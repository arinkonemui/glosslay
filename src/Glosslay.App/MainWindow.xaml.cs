using System.Windows;

namespace Glosslay;

/// <summary>
/// PoC の操作ウィンドウ（MainWindow.xaml のコードビハインド）。
/// UI の作り込みは v0.1 PoC の対象外（PLAN.md）。
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
