// CsWin32 の生成対象。ここに書いた API だけがビルド時に C# 生成される。
// 実行時 DLL は同梱されない（Glosslay.Core.csproj のコメント参照）。

// --- D3D11 デバイス ---
// WGC のフレームは GPU テクスチャで届くため、CPU へ読み出すのに D3D11 を経由する必要がある。
// （GPU"推論"は既定で無効という RULES.md の制約とは別の話。キャプチャ経路は WGC の仕様）
D3D11CreateDevice
D3D11_CREATE_DEVICE_FLAG
D3D_DRIVER_TYPE
ID3D11Device
ID3D11DeviceContext
ID3D11Texture2D
D3D11_TEXTURE2D_DESC
D3D11_USAGE
D3D11_BIND_FLAG
D3D11_CPU_ACCESS_FLAG
D3D11_MAP
D3D11_MAPPED_SUBRESOURCE
IDXGIDevice

// --- WinRT <-> D3D11 の相互変換 ---
// IDirect3DDevice の生成と、Direct3D11CaptureFrame.Surface からの ID3D11Texture2D 取り出し
CreateDirect3D11DeviceFromDXGIDevice
IDirect3DDxgiInterfaceAccess

// NOTE: IGraphicsCaptureItemInterop は不要。
// Windows 11 では GraphicsCaptureItem.TryCreateFromWindowId / TryCreateFromDisplayId が
// 射影されており、COM 相互運用なしで対象を作成できる（2026-09-08 に実機で確認済み）。

// --- オーバーレイウィンドウの拡張スタイル（FR-OVL-02 / 03 / 09） ---
// RULES.md が必須と定める LAYERED / TRANSPARENT / NOACTIVATE / TOOLWINDOW を設定する。
GetWindowLongPtrW
SetWindowLongPtrW
WINDOW_EX_STYLE
WINDOW_LONG_PTR_INDEX

// 対象がフォアグラウンドかの確認。ゲーム以外へ切り替えたらオーバーレイを引っ込めるために使う。
// 前面ウィンドウのハンドルを問い合わせるだけで、ゲームプロセスには一切触れない。
GetForegroundWindow

// --- APIキーのローカル暗号化（FR-TRN-05） ---
// DPAPI。現在のユーザーの資格情報で暗号化するため、鍵の管理を自前で行わずに済む。
// System.Security.Cryptography.ProtectedData パッケージでも同じことができるが、
// CsWin32 なら実行時 DLL が増えない（RULES.md 🔵「依存関係を増やすときは相談する」）。
CryptProtectData
CryptUnprotectData
CRYPT_INTEGER_BLOB
CRYPTPROTECT_UI_FORBIDDEN
LocalFree

// --- ホットキー（FR-INP-02「Raw Input で検知し、キー入力はゲームにそのまま流す」） ---
// RULES.md 🔴-3: RegisterHotKey（キーがゲームに届かなくなる）と
// WH_KEYBOARD_LL（キーロガー誤検知）は禁止。Raw Input が唯一の許可された方法。
// RIDEV_INPUTSINK は「前面でなくても受け取る」指定で、入力を横取りはしない。
RegisterRawInputDevices
GetRawInputData
RAWINPUTDEVICE
RAWINPUT
RAWINPUTHEADER
RAWKEYBOARD
RAWINPUTDEVICE_FLAGS
RAW_INPUT_DATA_COMMAND_FLAGS
HRAWINPUT
WM_INPUT
RI_KEY_BREAK
RID_DEVICE_INFO_TYPE

// --- キャプチャ対象の列挙（ウィンドウ指定 / モニタ全体の切替: FR-CAP-02） ---
EnumWindows
IsWindowVisible
GetWindowTextW
GetWindowTextLengthW
GetClientRect
GetWindowRect
MonitorFromWindow
EnumDisplayMonitors
GetMonitorInfoW
DwmGetWindowAttribute
DWMWINDOWATTRIBUTE

// --- カーソル位置モード（FR-MOD-09〜14） ---
// カーソルの周囲だけを OCR するために、押された瞬間の座標を 1 回だけ読む。
// 位置を問い合わせるだけで、ゲームプロセスには触れない（RULES.md 🔴-1）。
// 入力も奪わず、移動の監視もしない（🔴-3）。
GetCursorPos
